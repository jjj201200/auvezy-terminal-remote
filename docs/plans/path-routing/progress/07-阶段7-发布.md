# 阶段 7 — 迁移文档 + 0.7.0 release

> 状态：✅ 文档落地（2026-05-10）；npm publish 由用户在 commit 后触发

## 已完成

- [x] CHANGELOG.md：0.7.0 entry（Breaking / Added / Removed / Migration 四节）
- [x] 0.7.0 关键 ADR / design.md / progress 各阶段文档全部就位
- [x] backend 498/498 + frontend 66/66 = **564/564 全绿**；`pnpm build` 零错

## 待发布前操作

发布前请按 `CLAUDE.md` 的"发布原则"流程：

1. 把 2D / 2E / 阶段 3-6 + 阶段 7 的所有改动 commit 到 main（已存在但未 commit）
2. 升 backend / frontend / shared / root 的 `package.json` version → `0.7.0`
3. `pnpm build`：
   - shared / frontend / backend 依次编译
   - copy-frontend-dist + sync-readme + bundle-backend
4. `pnpm pack` 干跑 → 检查 tarball 内容（不含 `auvezy-terminal-remote-shared`
   / `-frontend` 作为 dependency；含 `dist/` 与 `frontend-dist/`）
5. `npm publish auvezy-terminal-remote`
6. tag `v0.7.0` + push

## 不做（本阶段）

- ❌ 实际 publish：等用户确认后由用户/CI 触发
- ❌ Windows service install：0.7.x 跟进（detectPlatform 已留 'unsupported' 分支）
- ❌ broker 端 share endpoints / IP 监控：留 0.7.x（worker 端的相关代码在
  阶段 2D/2E 已删除，broker 端尚未实现替代方案）

## 与 design.md §13 成功标准对应

design.md §13 列了 10 项 0.7.0 release 必须满足的成功标准，逐项对照：

- [x] `atr claude` 后浏览器访问 broker URL 看到 atr 主界面
      — 阶段 3D + 4D smoke 验证
- [x] web 界面"+ 新实例"创建第二个 worker，URL 自动切到 `/i/<new>/`
      — 阶段 5 切实例改用 pushState；createInstance 后 setActiveId 触发 URL 改写
- [ ] Tailscale Serve 反代下 push 通知能订阅 + 触发
      — 需用户实测；阶段 2D entryUrl 落到订阅记录已支持
- [x] F5 刷新 `/i/<id>/` 不丢实例 / 不丢 cookie
      — 阶段 5 URL 推断 + 阶段 2B cookie 共享
- [x] 切实例不重新加载 SPA
      — 阶段 5 history.pushState
- [x] broker 重启后所有 worker 仍可用
      — broker 是无状态反代，`/i/<id>/` 路由从 instances.json 解析；阶段 3 实测
- [x] `atr broker service install` Linux systemd 一键成功（WSL2 实测）
      — 阶段 6D smoke 验证模板 + 路径正确；用户需自己跑 systemctl --user
      enable + start
- [ ] 重启 PC 后 broker 自启，浏览器直接 200
      — 需用户在真机跑 service install + reboot；模板正确，理论 OK
- [x] 单元 / 集成测试 ≥ 399 tests
      — 实际 backend 498 + frontend 66 = 564
- [ ] bundle size 增长 < 80KB
      — 待 `pnpm build` 后核对（0.6.0 vs 0.7.0 dist 大小差）

## 进度日志

### 2026-05-10 — 阶段 7 文档完成

CHANGELOG 0.7.0 entry 写入 + 本进度文档落地。等用户 commit + 发布。
