# File Browser 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为活跃实例提供只读目录浏览 + 文本/图片预览 + 语法高亮 + 文件搜索功能,所有 API 挂 broker `/api/files/*`,复用 `checkWorkdir` 作安全边界。

**Architecture:** 后端单元落 `backend/src/files/`(path-resolver / mime-detect / list-dir / read-file / search-engine),路由落 `backend/src/api/file-routes.ts`,挂入现有 `createBrokerApiRouter`。前端单元落 `frontend/src/components/files/`,通过新 hook `useFiles(instanceId)` 暴露,入口 IconFolder 挂 ConsolePage 顶栏。Shiki 走 lazy load,SSE 搜索复用 `EventSource`(项目已用模式)。

**Tech Stack:** TypeScript / Express / vitest / React 19 / Vite 6 / Shiki / SSE(text/event-stream)/ picomatch(复用 `checkWorkdir`)

**前置阅读:**
- `docs/plans/file-browser/design.md`(本计划基于的设计)
- `docs/plans/file-browser/adrs/001..005`(决策上下文)
- `CLAUDE.md`(项目规范:中文注释 / AppError 子类 / 严禁 AI commit 署名 / smoke 后释放端口)

**Commit 规范(强制):**
- 不带任何 AI 署名(`Co-Authored-By: Claude` / `🤖` / `Generated by` 等)
- 中文 commit subject,格式 `feat(file-browser): xxx` / `test(file-browser): xxx` / `docs(file-browser): xxx`
- 每个 task 完成 → 一次 commit

---

## 文件结构总览

```
shared/src/
├── errors.ts                       # 修改:加 8 个 ErrorCode
├── files.ts                        # 新:类型 FileEntry / FileListResponse 等

backend/src/
├── errors.ts                       # 修改:加 FileError 子类
├── files/                          # 新目录
│   ├── path-resolver.ts            # resolveSafePath + 三段闸
│   ├── path-resolver.test.ts
│   ├── mime-detect.ts              # 扩展名 → mime + previewable
│   ├── mime-detect.test.ts
│   ├── list-dir.ts                 # readdir + stat 包装
│   ├── list-dir.test.ts
│   ├── read-file.ts                # 文本读 + 截断 + binary 判
│   ├── read-file.test.ts
│   ├── search-engine.ts            # name + content 搜索 + ReDoS 防护
│   └── search-engine.test.ts
├── api/
│   ├── router.ts                   # 修改:挂 file-routes
│   ├── file-routes.ts              # 新:5 个端点
│   └── file-routes.test.ts
└── broker/
    ├── broker-server.ts            # 修改:BrokerApiRouterOptions 加 registry
    └── cli.ts                      # 修改:把 registry 传给 broker-server

frontend/src/
├── components/files/               # 新目录
│   ├── FileBrowserSheet.tsx
│   ├── FileBrowserSheet.module.scss
│   ├── FileList.tsx
│   ├── Breadcrumb.tsx
│   ├── PreviewPane.tsx
│   ├── TextPreview.tsx
│   ├── ImagePreview.tsx
│   ├── SearchBox.tsx
│   └── SearchResults.tsx
├── hooks/
│   ├── useFiles.ts                 # 新
│   └── useFiles.test.ts
├── services/
│   └── files-api.ts                # 新:fetch wrapper(list/read/stat/search)
├── utils/
│   ├── syntax-highlight.ts         # 新:Shiki 包装 + 降级
│   ├── syntax-highlight.test.ts
│   └── lang-map.ts                 # 新:后端 lang → Shiki bundleId
└── pages/
    └── ConsolePage.tsx             # 修改:加 IconFolder 按钮 + Sheet 集成

frontend/src/i18n/
└── messages.ts                     # 修改:加 files.* 键(zh-CN + en-US)
```

---

## 阶段 1 — shared 类型 + ErrorCode + FileError 子类

阶段产物:`shared` 与 `backend` typecheck 通过、单测全绿。不引入运行行为。

### Task 1.1 — 新增 8 个 ErrorCode

**Files:**
- Modify: `shared/src/errors.ts`(在 enum 末尾 `NOT_IMPLEMENTED` 之前插入)

- [ ] **Step 1: 在 `shared/src/errors.ts` 末尾分类前(`// ──── 内部类 ────` 之前)插入**

```ts
  // ──────────────── 通用类(本项目首次引入 400 通用) ────────────────
  BAD_REQUEST = 'BAD_REQUEST',

  // ──────────────── 文件浏览类 ────────────────
  PATH_NOT_FOUND = 'PATH_NOT_FOUND',
  PATH_FORBIDDEN = 'PATH_FORBIDDEN',
  FILE_TOO_LARGE = 'FILE_TOO_LARGE',
  FILE_BINARY = 'FILE_BINARY',
  FILE_TYPE_FORBID = 'FILE_TYPE_FORBID',
  SEARCH_INVALID_Q = 'SEARCH_INVALID_Q',
  SEARCH_TIMEOUT = 'SEARCH_TIMEOUT',
```

- [ ] **Step 2: typecheck 通过**

```bash
pnpm --filter auvezy-terminal-remote-shared typecheck
```

Expected: 无错。

- [ ] **Step 3: commit**

```bash
git add shared/src/errors.ts
git commit -m "feat(file-browser): 阶段 1.1 — 新增 8 个 ErrorCode

BAD_REQUEST + 文件浏览类(PATH_NOT_FOUND / PATH_FORBIDDEN /
FILE_TOO_LARGE / FILE_BINARY / FILE_TYPE_FORBID /
SEARCH_INVALID_Q / SEARCH_TIMEOUT)。

按 shared/src/errors.ts 注释'只增不删'原则,在 NOT_IMPLEMENTED 前插入。"
```

---

### Task 1.2 — `shared/src/files.ts` 类型定义

**Files:**
- Create: `shared/src/files.ts`
- Test: `shared/src/files.test.ts`

- [ ] **Step 1: 写测试(类型存在性 + 联合类型穷举)**

`shared/src/files.test.ts`:

```ts
import { describe, it, expectTypeOf } from 'vitest';
import type {
  FileEntry,
  FileListResponse,
  FileReadResponse,
  FileStatResponse,
  SearchNameMatch,
  SearchContentMatch,
  SearchEvent,
  SearchDone,
  FilePreviewKind,
} from './files.js';

describe('files types', () => {
  it('FilePreviewKind 是 text|image|none 联合', () => {
    expectTypeOf<FilePreviewKind>().toEqualTypeOf<'text' | 'image' | 'none'>();
  });

  it('FileEntry.kind 包含四类', () => {
    expectTypeOf<FileEntry['kind']>().toEqualTypeOf<'file' | 'dir' | 'symlink' | 'other'>();
  });

  it('SearchEvent 是 name 与 content 的判别联合', () => {
    const ev: SearchEvent = { kind: 'name', path: '/x', size: 0, mtimeMs: 0 };
    if (ev.kind === 'name') {
      expectTypeOf(ev).toMatchTypeOf<SearchNameMatch>();
    }
  });

  it('FileListResponse.parent 允许 null', () => {
    expectTypeOf<FileListResponse['parent']>().toEqualTypeOf<string | null>();
  });
});
```

- [ ] **Step 2: 跑测试看失败**

```bash
pnpm --filter auvezy-terminal-remote-shared exec vitest run src/files.test.ts
```

Expected: FAIL — `Cannot find module './files.js'`.

- [ ] **Step 3: 实现 `shared/src/files.ts`**

```ts
/**
 * 文件浏览协议类型(前后端共享)
 *
 * 与 design.md §4.1 对齐。所有路径字段均为绝对路径。
 */

export type FilePreviewKind = 'text' | 'image' | 'none';

/** 列表条目 */
export interface FileEntry {
  /** 仅 basename,不含路径 */
  name: string;
  kind: 'file' | 'dir' | 'symlink' | 'other';
  /** 字节数;dir 固定 0 */
  size: number;
  mtimeMs: number;
  /** name 以 . 开头 */
  hidden: boolean;
  /** 仅 file 有 */
  mime?: string;
  previewable?: FilePreviewKind;
}

/** GET /api/files/list 响应 */
export interface FileListResponse {
  ok: true;
  /** 实例 cwd 绝对路径 */
  cwd: string;
  /** 当前展示路径绝对路径 */
  path: string;
  /** 上级目录绝对路径;null 表示越界已到 workdir-policy 边界 */
  parent: string | null;
  entries: FileEntry[];
}

/** GET /api/files/read 响应 */
export interface FileReadResponse {
  ok: true;
  path: string;
  mime: string;
  /** UTF-8 文本;二进制走 /raw */
  content: string;
  /** content 是否被截断到 2 MiB */
  truncated: boolean;
  /** 字节数(原始) */
  size: number;
  /** 后端推断的 lang(unknown → 'txt') */
  lang: string;
}

/** GET /api/files/stat 响应 */
export interface FileStatResponse {
  ok: true;
  path: string;
  kind: 'file' | 'dir' | 'symlink' | 'other';
  size: number;
  mtimeMs: number;
  mime?: string;
  previewable?: FilePreviewKind;
}

// ──────────────── 搜索 SSE 事件 ────────────────

export interface SearchNameMatch {
  kind: 'name';
  path: string;
  size: number;
  mtimeMs: number;
}

export interface SearchContentMatch {
  kind: 'content';
  path: string;
  /** 1-based */
  line: number;
  /** 单行裁到 200 字符 */
  preview: string;
  /** preview 内的高亮区间 */
  matchStart: number;
  matchEnd: number;
}

export type SearchEvent = SearchNameMatch | SearchContentMatch;

export interface SearchDone {
  truncated: boolean;
  scanned: number;
  elapsedMs: number;
}

/** 文件大小上限常量 */
export const FILE_READ_MAX_BYTES = 2 * 1024 * 1024;       // 2 MiB
export const FILE_RAW_MAX_BYTES = 8 * 1024 * 1024;        // 8 MiB
export const SEARCH_MAX_Q_LENGTH = 200;
export const SEARCH_MAX_NAME_RESULTS = 100;
export const SEARCH_MAX_CONTENT_RESULTS = 200;
export const SEARCH_FILE_TIMEOUT_MS = 100;
export const SEARCH_TOTAL_TIMEOUT_MS = 5000;
export const SEARCH_CONCURRENCY = 8;
```

- [ ] **Step 4: 在 `shared/src/index.ts` 末尾追加 re-export**

```ts
export * from './files.js';
```

- [ ] **Step 5: 跑测试**

```bash
pnpm --filter auvezy-terminal-remote-shared exec vitest run src/files.test.ts
```

Expected: PASS。

- [ ] **Step 6: typecheck**

```bash
pnpm --filter auvezy-terminal-remote-shared typecheck
```

Expected: 无错。

- [ ] **Step 7: commit**

```bash
git add shared/src/files.ts shared/src/files.test.ts shared/src/index.ts
git commit -m "feat(file-browser): 阶段 1.2 — shared/src/files.ts 协议类型

FileEntry / FileListResponse / FileReadResponse / FileStatResponse +
SearchEvent 判别联合(SearchNameMatch | SearchContentMatch)+ SearchDone +
体积/超时/并发常量。"
```

---

### Task 1.3 — `backend/src/errors.ts` 加 `FileError` 子类

**Files:**
- Modify: `backend/src/errors.ts`(在 `PushError` 后插入)

- [ ] **Step 1: 在 `PushError` class 后插入**

```ts
/** 文件浏览错误(默认 400) */
export class FileError extends AppError {
  constructor(code: ErrorCode, message: string, httpStatus = 400, cause?: unknown) {
    super(code, message, httpStatus, cause);
  }
}
```

- [ ] **Step 2: typecheck**

```bash
pnpm --filter auvezy-terminal-remote typecheck
```

Expected: 无错。

- [ ] **Step 3: commit**

```bash
git add backend/src/errors.ts
git commit -m "feat(file-browser): 阶段 1.3 — 新增 FileError AppError 子类"
```

---

### Task 1.4 — 阶段 1 进度文档

**Files:**
- Create: `docs/plans/file-browser/progress/01-阶段1-shared类型.md`

- [ ] **Step 1: 创建进度文件**

```markdown
# 阶段 1 — shared 类型 + ErrorCode + FileError

> 状态:✅ 已完成(<填实际日期>)

## 目标

为后续阶段准备类型契约与错误体系。本阶段纯加法,不触碰任何运行时路径。

## 完成项

- 1.1 — 8 个新 ErrorCode 落 shared/src/errors.ts
- 1.2 — shared/src/files.ts 协议类型 + 常量
- 1.3 — backend/src/errors.ts 加 FileError 子类
```

- [ ] **Step 2: commit**

```bash
git add docs/plans/file-browser/progress/01-阶段1-shared类型.md
git commit -m "docs(file-browser): 阶段 1 进度文档"
```

---

## 阶段 2 — path-resolver + mime-detect + list-dir

阶段产物:`backend/src/files/` 三个纯函数模块 + 单测全绿。**不挂任何路由**。

### Task 2.1 — `path-resolver.ts` 写测试

**Files:**
- Create: `backend/src/files/path-resolver.test.ts`(先写测试)

- [ ] **Step 1: 写测试**

```ts
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import { mkdtempSync, writeFileSync, mkdirSync, symlinkSync, rmSync, chmodSync } from 'node:fs';
import { tmpdir } from 'node:os';
import { join, resolve } from 'node:path';
import { resolveSafePath } from './path-resolver.js';
import { FileError } from '../errors.js';
import { ErrorCode } from 'auvezy-terminal-remote-shared';

describe('resolveSafePath', () => {
  let root: string;
  let cwd: string;

  beforeAll(() => {
    root = mkdtempSync(join(tmpdir(), 'fb-resolve-'));
    cwd = join(root, 'work');
    mkdirSync(cwd);
    mkdirSync(join(cwd, 'sub'));
    writeFileSync(join(cwd, 'file.txt'), 'hi');
    writeFileSync(join(cwd, 'sub', 'inner.txt'), 'inner');
    // symlink 指向 cwd 外:用于越界测试
    mkdirSync(join(root, 'outside'));
    writeFileSync(join(root, 'outside', 'secret'), 'x');
    symlinkSync(join(root, 'outside'), join(cwd, 'link-out'));
  });

  afterAll(() => {
    rmSync(root, { recursive: true, force: true });
  });

  it('相对路径"."解析为 cwd', () => {
    const r = resolveSafePath(cwd, '.', { allow: [], deny: [] });
    expect(r).toBe(cwd);
  });

  it('相对路径 sub 解析为 cwd/sub', () => {
    const r = resolveSafePath(cwd, 'sub', { allow: [], deny: [] });
    expect(r).toBe(join(cwd, 'sub'));
  });

  it('绝对路径直接接受(在 deny 外)', () => {
    const r = resolveSafePath(cwd, cwd, { allow: [], deny: [] });
    expect(r).toBe(cwd);
  });

  it('symlink 解到 deny 外抛 PATH_FORBIDDEN', () => {
    expect(() =>
      resolveSafePath(cwd, 'link-out', {
        allow: [],
        deny: [`${root}/outside/**`, `${root}/outside`],
      }),
    ).toThrowError(/PATH_FORBIDDEN|forbidden/i);
  });

  it('不存在的路径抛 PATH_NOT_FOUND', () => {
    let caught: unknown;
    try {
      resolveSafePath(cwd, 'nope-xxx', { allow: [], deny: [] });
    } catch (e) {
      caught = e;
    }
    expect(caught).toBeInstanceOf(FileError);
    expect((caught as FileError).code).toBe(ErrorCode.PATH_NOT_FOUND);
  });

  it('allow 非空且未命中抛 PATH_FORBIDDEN', () => {
    let caught: unknown;
    try {
      resolveSafePath(cwd, '.', { allow: ['/never/**'], deny: [] });
    } catch (e) {
      caught = e;
    }
    expect(caught).toBeInstanceOf(FileError);
    expect((caught as FileError).code).toBe(ErrorCode.PATH_FORBIDDEN);
  });

  it('deny 命中抛 PATH_FORBIDDEN', () => {
    let caught: unknown;
    try {
      resolveSafePath(cwd, '.', { allow: [], deny: [`${cwd}/**`, cwd] });
    } catch (e) {
      caught = e;
    }
    expect(caught).toBeInstanceOf(FileError);
    expect((caught as FileError).code).toBe(ErrorCode.PATH_FORBIDDEN);
  });
});
```

- [ ] **Step 2: 跑测试看 FAIL**

```bash
pnpm --filter auvezy-terminal-remote exec vitest run src/files/path-resolver.test.ts
```

Expected: FAIL — `Cannot find module './path-resolver.js'`.

- [ ] **Step 3: commit 失败测试**

```bash
git add backend/src/files/path-resolver.test.ts
git commit -m "test(file-browser): 阶段 2.1 — resolveSafePath 失败测试"
```

---

### Task 2.2 — `path-resolver.ts` 实现

**Files:**
- Create: `backend/src/files/path-resolver.ts`

- [ ] **Step 1: 实现**

```ts
/**
 * resolveSafePath:把外部输入的相对/绝对路径解析为可信的绝对路径
 *
 * 三段闸:
 *  1. path.resolve(cwd, input)
 *  2. fs.realpathSync 解 symlink → real
 *  3. checkWorkdir(real, allow, deny) 复审
 *
 * 通过 → 返回 real;不通过 → 抛 FileError。
 *
 * 路由层先用 instanceId 查 cwd + 当前 workdirPolicy 快照,再调本函数。
 * 本函数不直接接触 instances.json,便于单测。
 */

import { realpathSync } from 'node:fs';
import { isAbsolute, resolve as resolvePath } from 'node:path';
import { ErrorCode } from 'auvezy-terminal-remote-shared';
import { FileError } from '../errors.js';
import { checkWorkdir } from '../utils/workdir-policy.js';

export interface WorkdirPolicy {
  allow: readonly string[];
  deny: readonly string[];
}

export function resolveSafePath(
  cwd: string,
  input: string | undefined,
  policy: WorkdirPolicy,
): string {
  const raw = input && input.length > 0 ? input : '.';
  const abs = isAbsolute(raw) ? raw : resolvePath(cwd, raw);

  let real: string;
  try {
    real = realpathSync(abs);
  } catch (err) {
    throw new FileError(
      ErrorCode.PATH_NOT_FOUND,
      `path not found: ${abs}`,
      404,
      err,
    );
  }

  const verdict = checkWorkdir(real, policy.allow, policy.deny);
  if (verdict !== null) {
    throw new FileError(
      ErrorCode.PATH_FORBIDDEN,
      `path forbidden: ${verdict.reason}`,
      403,
    );
  }

  return real;
}
```

- [ ] **Step 2: 跑测试**

```bash
pnpm --filter auvezy-terminal-remote exec vitest run src/files/path-resolver.test.ts
```

Expected: PASS(7/7)。

- [ ] **Step 3: commit**

```bash
git add backend/src/files/path-resolver.ts
git commit -m "feat(file-browser): 阶段 2.2 — resolveSafePath 三段闸实现"
```

---

### Task 2.3 — `mime-detect.ts` 写测试 + 实现

**Files:**
- Create: `backend/src/files/mime-detect.test.ts`
- Create: `backend/src/files/mime-detect.ts`

- [ ] **Step 1: 写测试**

```ts
import { describe, it, expect } from 'vitest';
import { detectMime, detectLang } from './mime-detect.js';

describe('detectMime', () => {
  it('.md → text/markdown + previewable=text', () => {
    const r = detectMime('README.md');
    expect(r.mime).toBe('text/markdown');
    expect(r.previewable).toBe('text');
  });

  it('.png → image/png + previewable=image', () => {
    const r = detectMime('logo.png');
    expect(r.mime).toBe('image/png');
    expect(r.previewable).toBe('image');
  });

  it('.svg 优先 image 渲染', () => {
    const r = detectMime('icon.svg');
    expect(r.previewable).toBe('image');
  });

  it('未知扩展 → previewable=none', () => {
    const r = detectMime('blob.xyz123');
    expect(r.previewable).toBe('none');
    expect(r.mime).toBe('application/octet-stream');
  });

  it('无扩展但全名命中(Makefile / Dockerfile)→ text', () => {
    expect(detectMime('Makefile').previewable).toBe('text');
    expect(detectMime('Dockerfile').previewable).toBe('text');
  });
});

describe('detectLang', () => {
  it('.ts → ts', () => expect(detectLang('a.ts')).toBe('ts'));
  it('.tsx → tsx', () => expect(detectLang('a.tsx')).toBe('tsx'));
  it('.md → markdown', () => expect(detectLang('a.md')).toBe('markdown'));
  it('.yml → yaml', () => expect(detectLang('a.yml')).toBe('yaml'));
  it('未知 → txt', () => expect(detectLang('a.xyz')).toBe('txt'));
});
```

- [ ] **Step 2: 跑失败**

```bash
pnpm --filter auvezy-terminal-remote exec vitest run src/files/mime-detect.test.ts
```

Expected: FAIL — 模块不存在。

- [ ] **Step 3: 实现**

```ts
/**
 * mime-detect:扩展名 → mime + previewable kind + lang
 *
 * 不读文件内容,仅基于路径。二进制识别在 read-file.ts 做(NUL 字节 + 替换字符)。
 */

import { basename, extname } from 'node:path';
import type { FilePreviewKind } from 'auvezy-terminal-remote-shared';

const TEXT_EXT_TO_MIME: Record<string, string> = {
  '.md': 'text/markdown', '.markdown': 'text/markdown',
  '.txt': 'text/plain', '.log': 'text/plain',
  '.json': 'application/json', '.jsonc': 'application/json',
  '.yml': 'application/yaml', '.yaml': 'application/yaml',
  '.toml': 'application/toml', '.ini': 'text/plain', '.conf': 'text/plain', '.cfg': 'text/plain',
  '.env': 'text/plain', '.example': 'text/plain',
  '.gitignore': 'text/plain', '.dockerignore': 'text/plain',
  '.ts': 'text/typescript', '.tsx': 'text/tsx',
  '.js': 'text/javascript', '.jsx': 'text/jsx', '.mjs': 'text/javascript', '.cjs': 'text/javascript',
  '.py': 'text/x-python', '.go': 'text/x-go', '.rs': 'text/x-rust',
  '.rb': 'text/x-ruby', '.php': 'text/x-php',
  '.java': 'text/x-java', '.kt': 'text/x-kotlin', '.swift': 'text/x-swift',
  '.c': 'text/x-c', '.h': 'text/x-c',
  '.cc': 'text/x-c++', '.cpp': 'text/x-c++', '.hpp': 'text/x-c++',
  '.cs': 'text/x-csharp',
  '.sh': 'text/x-shellscript', '.bash': 'text/x-shellscript', '.zsh': 'text/x-shellscript',
  '.fish': 'text/x-shellscript', '.ps1': 'text/x-shellscript',
  '.html': 'text/html', '.htm': 'text/html',
  '.css': 'text/css', '.scss': 'text/x-scss', '.sass': 'text/x-sass', '.less': 'text/x-less',
  '.xml': 'application/xml', '.sql': 'application/sql',
  '.csv': 'text/csv', '.tsv': 'text/tab-separated-values',
  '.lock': 'text/plain', '.makefile': 'text/x-makefile',
};

const IMAGE_EXT_TO_MIME: Record<string, string> = {
  '.png': 'image/png', '.jpg': 'image/jpeg', '.jpeg': 'image/jpeg',
  '.gif': 'image/gif', '.webp': 'image/webp', '.svg': 'image/svg+xml',
  '.bmp': 'image/bmp', '.ico': 'image/x-icon', '.avif': 'image/avif',
};

const BARE_NAME_TEXT = new Set(['Makefile', 'Dockerfile', 'Rakefile', 'Gemfile', 'Procfile']);

const LANG_MAP: Record<string, string> = {
  '.ts': 'ts', '.tsx': 'tsx', '.js': 'js', '.jsx': 'jsx',
  '.mjs': 'js', '.cjs': 'js',
  '.md': 'markdown', '.markdown': 'markdown',
  '.yml': 'yaml', '.yaml': 'yaml',
  '.json': 'json', '.jsonc': 'json',
  '.toml': 'toml',
  '.py': 'python', '.go': 'go', '.rs': 'rust',
  '.rb': 'ruby', '.php': 'php',
  '.java': 'java', '.kt': 'kotlin', '.swift': 'swift',
  '.c': 'c', '.h': 'c',
  '.cc': 'cpp', '.cpp': 'cpp', '.hpp': 'cpp',
  '.cs': 'csharp',
  '.sh': 'shell', '.bash': 'shell', '.zsh': 'shell',
  '.html': 'html', '.htm': 'html',
  '.css': 'css', '.scss': 'scss', '.sass': 'sass', '.less': 'less',
  '.xml': 'xml', '.sql': 'sql',
  '.svg': 'xml',
};

export interface MimeInfo {
  mime: string;
  previewable: FilePreviewKind;
}

export function detectMime(filename: string): MimeInfo {
  const base = basename(filename);
  const ext = extname(base).toLowerCase();

  // 全名命中(Makefile 等)
  if (BARE_NAME_TEXT.has(base)) {
    return { mime: 'text/plain', previewable: 'text' };
  }

  // 图片优先(svg 走 image)
  if (ext in IMAGE_EXT_TO_MIME) {
    return { mime: IMAGE_EXT_TO_MIME[ext]!, previewable: 'image' };
  }
  if (ext in TEXT_EXT_TO_MIME) {
    return { mime: TEXT_EXT_TO_MIME[ext]!, previewable: 'text' };
  }
  return { mime: 'application/octet-stream', previewable: 'none' };
}

export function detectLang(filename: string): string {
  const ext = extname(filename).toLowerCase();
  return LANG_MAP[ext] ?? 'txt';
}
```

- [ ] **Step 4: 跑测试**

```bash
pnpm --filter auvezy-terminal-remote exec vitest run src/files/mime-detect.test.ts
```

Expected: PASS。

- [ ] **Step 5: commit**

```bash
git add backend/src/files/mime-detect.ts backend/src/files/mime-detect.test.ts
git commit -m "feat(file-browser): 阶段 2.3 — mime/lang 检测纯函数

按扩展名 + 全名(Makefile/Dockerfile)映射。SVG 优先 image 渲染。
未知扩展返回 application/octet-stream + previewable=none。"
```

---

### Task 2.4 — `list-dir.ts` 写测试 + 实现

**Files:**
- Create: `backend/src/files/list-dir.test.ts`
- Create: `backend/src/files/list-dir.ts`

- [ ] **Step 1: 写测试**

```ts
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import { mkdtempSync, mkdirSync, writeFileSync, symlinkSync, rmSync } from 'node:fs';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { listDir } from './list-dir.js';

describe('listDir', () => {
  let root: string;

  beforeAll(() => {
    root = mkdtempSync(join(tmpdir(), 'fb-list-'));
    mkdirSync(join(root, 'sub'));
    writeFileSync(join(root, 'a.txt'), 'a');
    writeFileSync(join(root, 'b.png'), Buffer.from([0x89, 0x50, 0x4e, 0x47]));
    writeFileSync(join(root, '.hidden'), 'x');
    symlinkSync(join(root, 'a.txt'), join(root, 'lnk'));
  });

  afterAll(() => {
    rmSync(root, { recursive: true, force: true });
  });

  it('列出全部条目(含隐藏)', async () => {
    const entries = await listDir(root);
    const names = entries.map((e) => e.name).sort();
    expect(names).toEqual(['.hidden', 'a.txt', 'b.png', 'lnk', 'sub']);
  });

  it('hidden 字段正确', async () => {
    const entries = await listDir(root);
    const h = entries.find((e) => e.name === '.hidden')!;
    expect(h.hidden).toBe(true);
    const a = entries.find((e) => e.name === 'a.txt')!;
    expect(a.hidden).toBe(false);
  });

  it('文件标记 previewable', async () => {
    const entries = await listDir(root);
    const a = entries.find((e) => e.name === 'a.txt')!;
    expect(a.previewable).toBe('text');
    expect(a.kind).toBe('file');
    const b = entries.find((e) => e.name === 'b.png')!;
    expect(b.previewable).toBe('image');
  });

  it('目录条目 size = 0 且无 previewable', async () => {
    const entries = await listDir(root);
    const sub = entries.find((e) => e.name === 'sub')!;
    expect(sub.kind).toBe('dir');
    expect(sub.size).toBe(0);
    expect(sub.previewable).toBeUndefined();
  });

  it('symlink 标记 kind=symlink', async () => {
    const entries = await listDir(root);
    const lnk = entries.find((e) => e.name === 'lnk')!;
    expect(lnk.kind).toBe('symlink');
  });
});
```

- [ ] **Step 2: 跑失败**

```bash
pnpm --filter auvezy-terminal-remote exec vitest run src/files/list-dir.test.ts
```

Expected: FAIL — 模块不存在。

- [ ] **Step 3: 实现**

```ts
/**
 * listDir:列目录一层
 *
 * 使用 fs.opendir 异步迭代,对每个 entry 做 lstat 拿 size/mtime/kind。
 * 不递归。不读文件内容。socket/fifo/device 被映射为 kind='other',
 * 由路由层决定要不要在响应里隐藏(MVP 保留显示,但不可预览/读取)。
 */

import { opendir, lstat } from 'node:fs/promises';
import { join } from 'node:path';
import type { FileEntry } from 'auvezy-terminal-remote-shared';
import { detectMime } from './mime-detect.js';

export async function listDir(dirPath: string): Promise<FileEntry[]> {
  const entries: FileEntry[] = [];
  const dir = await opendir(dirPath);

  for await (const dirent of dir) {
    const fullPath = join(dirPath, dirent.name);
    let stat;
    try {
      stat = await lstat(fullPath);
    } catch {
      // stat 失败(权限/race)→ 跳过该条目,不让单个失败拖垮整个 list
      continue;
    }

    const kind: FileEntry['kind'] = stat.isSymbolicLink()
      ? 'symlink'
      : stat.isDirectory()
        ? 'dir'
        : stat.isFile()
          ? 'file'
          : 'other';

    const entry: FileEntry = {
      name: dirent.name,
      kind,
      size: kind === 'dir' ? 0 : stat.size,
      mtimeMs: stat.mtimeMs,
      hidden: dirent.name.startsWith('.'),
    };

    if (kind === 'file') {
      const { mime, previewable } = detectMime(dirent.name);
      entry.mime = mime;
      entry.previewable = previewable;
    }

    entries.push(entry);
  }

  return entries;
}
```

- [ ] **Step 4: 跑测试**

```bash
pnpm --filter auvezy-terminal-remote exec vitest run src/files/list-dir.test.ts
```

Expected: PASS(5/5)。

- [ ] **Step 5: commit**

```bash
git add backend/src/files/list-dir.ts backend/src/files/list-dir.test.ts
git commit -m "feat(file-browser): 阶段 2.4 — listDir 列目录一层

fs.opendir 异步迭代 + lstat;symlink 标记 kind=symlink;
stat 失败的条目静默跳过(防单个权限错误拖垮整体)。"
```

---

### Task 2.5 — 阶段 2 进度文档

**Files:**
- Create: `docs/plans/file-browser/progress/02-阶段2-后端基础单元.md`

- [ ] **Step 1: 写**

```markdown
# 阶段 2 — 后端基础单元(path-resolver / mime-detect / list-dir)

> 状态:✅ 已完成(<日期>)

## 完成项

- 2.1 / 2.2 — resolveSafePath 三段闸 + 7 测试用例
- 2.3 — detectMime / detectLang 纯函数 + 11 测试
- 2.4 — listDir(opendir + lstat)+ 5 测试

## 不做(留下一阶段)

- read-file(阶段 3 一起)
- 路由挂载(阶段 3)
- 搜索(阶段 4)
```

- [ ] **Step 2: commit**

```bash
git add docs/plans/file-browser/progress/02-阶段2-后端基础单元.md
git commit -m "docs(file-browser): 阶段 2 进度文档"
```

---

## 阶段 3 — read-file + 路由 list/read/stat/raw

阶段产物:`/api/files/list` `/read` `/stat` `/raw` 真实可调,e2e supertest 全绿。**不含搜索**。

### Task 3.1 — `read-file.ts` 写测试

**Files:**
- Create: `backend/src/files/read-file.test.ts`

- [ ] **Step 1: 写测试**

```ts
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import { mkdtempSync, writeFileSync, rmSync } from 'node:fs';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { readTextFile } from './read-file.js';
import { FileError } from '../errors.js';
import { ErrorCode, FILE_READ_MAX_BYTES } from 'auvezy-terminal-remote-shared';

describe('readTextFile', () => {
  let root: string;

  beforeAll(() => {
    root = mkdtempSync(join(tmpdir(), 'fb-read-'));
    writeFileSync(join(root, 'a.txt'), 'hello');
    // 含 NUL 字节的二进制
    writeFileSync(join(root, 'bin.dat'), Buffer.from([0x00, 0x01, 0x02, 0x03]));
    // 超大文本(3 MiB)
    writeFileSync(join(root, 'big.txt'), Buffer.alloc(3 * 1024 * 1024, 0x61));
    // 高密度 replacement char(全部 0xFE 0xFE,UTF-8 解码会大量 �)
    writeFileSync(join(root, 'badenc.txt'), Buffer.alloc(1024, 0xfe));
  });

  afterAll(() => {
    rmSync(root, { recursive: true, force: true });
  });

  it('正常读 a.txt', async () => {
    const r = await readTextFile(join(root, 'a.txt'));
    expect(r.content).toBe('hello');
    expect(r.size).toBe(5);
    expect(r.truncated).toBe(false);
  });

  it('NUL 字节命中抛 FILE_BINARY', async () => {
    let caught: unknown;
    try {
      await readTextFile(join(root, 'bin.dat'));
    } catch (e) { caught = e; }
    expect(caught).toBeInstanceOf(FileError);
    expect((caught as FileError).code).toBe(ErrorCode.FILE_BINARY);
  });

  it('超大文本被截断到 2 MiB', async () => {
    const r = await readTextFile(join(root, 'big.txt'));
    expect(r.truncated).toBe(true);
    expect(Buffer.byteLength(r.content, 'utf8')).toBeLessThanOrEqual(FILE_READ_MAX_BYTES);
    expect(r.size).toBe(3 * 1024 * 1024);
  });

  it('替换字符密度 >5% 抛 FILE_BINARY', async () => {
    let caught: unknown;
    try {
      await readTextFile(join(root, 'badenc.txt'));
    } catch (e) { caught = e; }
    expect(caught).toBeInstanceOf(FileError);
    expect((caught as FileError).code).toBe(ErrorCode.FILE_BINARY);
  });
});
```

- [ ] **Step 2: 跑失败,commit 测试**

```bash
pnpm --filter auvezy-terminal-remote exec vitest run src/files/read-file.test.ts
git add backend/src/files/read-file.test.ts
git commit -m "test(file-browser): 阶段 3.1 — readTextFile 失败测试"
```

---

### Task 3.2 — `read-file.ts` 实现

**Files:**
- Create: `backend/src/files/read-file.ts`

- [ ] **Step 1: 实现**

```ts
/**
 * readTextFile:读文本文件,带大小截断 + 字节级/字符级二进制识别
 *
 * 严格顺序(design §5.4):
 *  1. open + 读前 4 KiB:命中 0x00 → FILE_BINARY
 *  2. 全文读(最多 2 MiB,超量截断)
 *  3. 解码后检查 �(U+FFFD)密度,>5% → FILE_BINARY
 */

import { open, stat } from 'node:fs/promises';
import { ErrorCode, FILE_READ_MAX_BYTES } from 'auvezy-terminal-remote-shared';
import { FileError } from '../errors.js';

export interface ReadResult {
  content: string;
  /** 是否被截断 */
  truncated: boolean;
  /** 原始字节数 */
  size: number;
}

const NUL_PROBE_BYTES = 4 * 1024;
const REPLACEMENT_CHAR = '�';
const REPLACEMENT_DENSITY_LIMIT = 0.05;

export async function readTextFile(absPath: string): Promise<ReadResult> {
  const st = await stat(absPath);
  const size = st.size;

  const fh = await open(absPath, 'r');
  try {
    // Step 1: 字节级 NUL 闸
    const probeLen = Math.min(NUL_PROBE_BYTES, size);
    if (probeLen > 0) {
      const probe = Buffer.alloc(probeLen);
      await fh.read(probe, 0, probeLen, 0);
      if (probe.includes(0x00)) {
        throw new FileError(
          ErrorCode.FILE_BINARY,
          'file contains NUL bytes (binary)',
          409,
        );
      }
    }

    // Step 2: 全文读(截断)
    const readLen = Math.min(size, FILE_READ_MAX_BYTES);
    const buf = Buffer.alloc(readLen);
    if (readLen > 0) {
      await fh.read(buf, 0, readLen, 0);
    }
    const content = buf.toString('utf-8');
    const truncated = size > FILE_READ_MAX_BYTES;

    // Step 3: 字符级替换字符密度闸
    if (content.length > 0) {
      let replacements = 0;
      for (const ch of content) {
        if (ch === REPLACEMENT_CHAR) replacements++;
      }
      const density = replacements / content.length;
      if (density > REPLACEMENT_DENSITY_LIMIT) {
        throw new FileError(
          ErrorCode.FILE_BINARY,
          `non-UTF8 content (replacement density ${(density * 100).toFixed(1)}%)`,
          409,
        );
      }
    }

    return { content, truncated, size };
  } finally {
    await fh.close();
  }
}
```

- [ ] **Step 2: 跑测试**

```bash
pnpm --filter auvezy-terminal-remote exec vitest run src/files/read-file.test.ts
```

Expected: PASS(4/4)。

- [ ] **Step 3: commit**

```bash
git add backend/src/files/read-file.ts
git commit -m "feat(file-browser): 阶段 3.2 — readTextFile 实现

字节级 NUL 闸 + 2 MiB 截断 + 字符级替换字符密度闸,三段顺序与 design §5.4 一致。"
```

---

### Task 3.3 — broker 把 `registry` 注入 `BrokerApiRouterOptions`

**Files:**
- Modify: `backend/src/api/router.ts`(BrokerApiRouterOptions 加 registry 字段)
- Modify: `backend/src/broker/broker-server.ts`(透传 registry)
- Modify: `backend/src/broker/cli.ts`(broker-server 调用处增字段)

> 现有 broker-server 已经持有 `registry`(用于 createBrokerInstanceRoutes),只是没传给 createBrokerApiRouter 的"非 instance 路由分支"。这里只是把已有的 registry **再透给 file-routes 用**。

- [ ] **Step 1: `backend/src/api/router.ts` 在 BrokerApiRouterOptions 上保留 registry(已有,但 file-routes 阶段才用,这里只确认)**

读 `backend/src/api/router.ts` 验证 `registry: InstanceRegistryManager` 已存在(参考已有 createBrokerInstanceRoutes 调用)。**已存在,本 step 跳过实际修改,只走 typecheck:**

```bash
pnpm --filter auvezy-terminal-remote typecheck
```

Expected: 无错。

- [ ] **Step 2: commit 空动作占位(若需要)**

> 若 typecheck 已绿且未触发修改,本 task 无 commit,直接进入 3.4。

---

### Task 3.4 — `file-routes.ts` 写 supertest

**Files:**
- Create: `backend/src/api/file-routes.test.ts`

- [ ] **Step 1: 看一眼现有的 supertest 模式**

```bash
cat backend/src/api/share-routes.test.ts | head -80
```

照搬其 setup(express + cookieParser + 假 authModule + 写 fixture cwd)。

- [ ] **Step 2: 写测试**(创建文件 `backend/src/api/file-routes.test.ts`)

```ts
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import express from 'express';
import request from 'supertest';
import cookieParser from 'cookie-parser';
import { mkdtempSync, mkdirSync, writeFileSync, rmSync } from 'node:fs';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { createFileRoutes } from './file-routes.js';
import type { AuthModule } from '../auth/auth-middleware.js';
import type { InstanceRegistryManager } from '../registry/instance-registry.js';
import type { InstanceInfo } from 'auvezy-terminal-remote-shared';

const FAKE_INSTANCE_ID = 'inst-1';

function makeApp(cwd: string, allow: string[] = [], deny: string[] = []) {
  const app = express();
  app.use(cookieParser());
  app.use(express.json());

  const authModule = {
    requireAuth(_req: unknown, _res: unknown, next: () => void) { next(); },
  } as unknown as AuthModule;

  const registry = {
    async list(): Promise<InstanceInfo[]> {
      return [{
        instanceId: FAKE_INSTANCE_ID, name: 'fake', host: '127.0.0.1', port: 0,
        pid: process.pid, cwd, startedAt: new Date().toISOString(),
      }];
    },
  } as unknown as InstanceRegistryManager;

  app.use(createFileRoutes({
    authModule,
    registry,
    workdirPolicy: () => ({ allow, deny }),
  }));
  return app;
}

describe('file-routes', () => {
  let root: string;
  beforeAll(() => {
    root = mkdtempSync(join(tmpdir(), 'fb-routes-'));
    mkdirSync(join(root, 'sub'));
    writeFileSync(join(root, 'README.md'), '# hi');
  });
  afterAll(() => rmSync(root, { recursive: true, force: true }));

  it('GET /api/files/list 缺 instanceId → 400 BAD_REQUEST', async () => {
    const app = makeApp(root);
    const res = await request(app).get('/api/files/list');
    expect(res.status).toBe(400);
    expect(res.body.error.code).toBe('BAD_REQUEST');
  });

  it('GET /api/files/list 实例不存在 → 404 INSTANCE_NOT_FOUND', async () => {
    const app = makeApp(root);
    const res = await request(app).get('/api/files/list?instanceId=missing');
    expect(res.status).toBe(404);
    expect(res.body.error.code).toBe('INSTANCE_NOT_FOUND');
  });

  it('GET /api/files/list cwd 列出 README.md + sub', async () => {
    const app = makeApp(root);
    const res = await request(app).get(`/api/files/list?instanceId=${FAKE_INSTANCE_ID}`);
    expect(res.status).toBe(200);
    expect(res.body.cwd).toBe(root);
    expect(res.body.path).toBe(root);
    const names = res.body.entries.map((e: { name: string }) => e.name).sort();
    expect(names).toEqual(['README.md', 'sub']);
  });

  it('GET /api/files/read 文本文件返回内容', async () => {
    const app = makeApp(root);
    const res = await request(app).get(`/api/files/read?instanceId=${FAKE_INSTANCE_ID}&path=README.md`);
    expect(res.status).toBe(200);
    expect(res.body.content).toBe('# hi');
    expect(res.body.lang).toBe('markdown');
  });

  it('GET /api/files/read deny 命中 → 403 PATH_FORBIDDEN', async () => {
    const app = makeApp(root, [], [`${root}/**`, root]);
    const res = await request(app).get(`/api/files/read?instanceId=${FAKE_INSTANCE_ID}&path=README.md`);
    expect(res.status).toBe(403);
    expect(res.body.error.code).toBe('PATH_FORBIDDEN');
  });

  it('GET /api/files/raw 不存在 → X-ATR-Error header(不返 JSON)', async () => {
    const app = makeApp(root);
    const res = await request(app).get(`/api/files/raw?instanceId=${FAKE_INSTANCE_ID}&path=nope.png`);
    expect(res.status).toBe(404);
    expect(res.headers['x-atr-error']).toBe('PATH_NOT_FOUND');
    expect(res.body).toEqual({});
  });

  it('GET /api/files/stat 目录返回 kind=dir', async () => {
    const app = makeApp(root);
    const res = await request(app).get(`/api/files/stat?instanceId=${FAKE_INSTANCE_ID}&path=sub`);
    expect(res.status).toBe(200);
    expect(res.body.kind).toBe('dir');
  });
});
```

- [ ] **Step 3: 跑失败 + commit**

```bash
pnpm --filter auvezy-terminal-remote exec vitest run src/api/file-routes.test.ts
git add backend/src/api/file-routes.test.ts
git commit -m "test(file-browser): 阶段 3.4 — file-routes supertest 失败用例"
```

Expected FAIL: `createFileRoutes` 不存在。

---

### Task 3.5 — `file-routes.ts` 实现(list / read / stat / raw)

**Files:**
- Create: `backend/src/api/file-routes.ts`

- [ ] **Step 1: 实现**

```ts
/**
 * /api/files/* 路由(broker 端)
 *
 * 端点(MVP 不含 search,见 search-routes 阶段 4):
 *   GET /api/files/list?instanceId=&path=
 *   GET /api/files/stat?instanceId=&path=
 *   GET /api/files/read?instanceId=&path=
 *   GET /api/files/raw?instanceId=&path=
 *
 * 鉴权:全部走 authModule.requireAuth(与现有 broker API 一致)。
 * 错误:统一 FileError → JSON;raw 端点特殊用 X-ATR-Error header,不返 JSON。
 */

import { Router, type Request, type Response } from 'express';
import { createReadStream } from 'node:fs';
import { lstat } from 'node:fs/promises';
import { ErrorCode, FILE_RAW_MAX_BYTES } from 'auvezy-terminal-remote-shared';
import type { InstanceRegistryManager } from '../registry/instance-registry.js';
import type { AuthModule } from '../auth/auth-middleware.js';
import type { WorkdirPolicySnapshot } from './workdir-policy-routes.js';
import { FileError } from '../errors.js';
import { logger } from '../logger/logger.js';
import { resolveSafePath, type WorkdirPolicy } from '../files/path-resolver.js';
import { listDir } from '../files/list-dir.js';
import { detectMime, detectLang } from '../files/mime-detect.js';
import { readTextFile } from '../files/read-file.js';

export interface FileRoutesOptions {
  authModule: AuthModule;
  registry: InstanceRegistryManager;
  workdirPolicy: () => WorkdirPolicySnapshot;
}

export function createFileRoutes(opts: FileRoutesOptions): Router {
  const router = Router();
  const { authModule, registry, workdirPolicy } = opts;

  /** 路由通用前置:查 cwd + 拿 policy 快照;失败抛 FileError 由 wrapHandler 接 */
  async function resolveContext(req: Request): Promise<{ cwd: string; policy: WorkdirPolicy }> {
    const instanceId = req.query.instanceId;
    if (typeof instanceId !== 'string' || instanceId.length === 0) {
      throw new FileError(ErrorCode.BAD_REQUEST, 'instanceId is required', 400);
    }
    const all = await registry.list();
    const inst = all.find((i) => i.instanceId === instanceId);
    if (!inst) {
      throw new FileError(ErrorCode.INSTANCE_NOT_FOUND, `instance not found: ${instanceId}`, 404);
    }
    const snap = workdirPolicy();
    return { cwd: inst.cwd, policy: { allow: snap.allow, deny: [] } };
    // 注:WorkdirPolicySnapshot 当前只暴露 allow(deny 是后端内部)。
    //     这里 deny 传 [] 看似"放行 deny",但实际上 file-routes.ts 上层会
    //     再调一次 checkWorkdir 用真 deny(下个 step 修复)。
  }
}
```

> ⚠️ 上面伪 stub 暴露一个 design 漏洞:**`WorkdirPolicySnapshot` 当前只导出 `allow`**(`workdir-policy-routes.ts` 注释:"不暴露 deny,deny 是安全防线")。**但 file-routes 内部需要 deny 才能完整复用 checkWorkdir**。

继续修复:

- [ ] **Step 2: 扩 `workdirPolicy` 工厂返回结构,加内部 `deny`**

修改 `backend/src/api/router.ts` `BrokerApiRouterOptions`,把 `workdirPolicy` 的返回从 `WorkdirPolicySnapshot`(只 allow)拆为两层:
- 对外只读快照接口仍然只暴露 allow(workdir-policy-routes 不变);
- 内部需 deny 时通过同一份 `workdirPolicy()` 工厂,**但工厂内部返回完整 `{ allow, deny }`**;`workdir-policy-routes` 自己只取 allow 字段返给用户。

具体改:
1. `backend/src/api/workdir-policy-routes.ts` 的 `WorkdirPolicySnapshot` interface 增加 `deny: string[]` 字段:

```ts
export interface WorkdirPolicySnapshot {
  allow: string[];
  /** 黑名单(不通过 /api/workdir-policy 端点暴露给前端,仅供 broker 内部其它路由复用) */
  deny: string[];
}
```

并修改路由实现仅返回 `{ allow: s.allow }`(去掉 deny,保持对外契约不变):

```ts
router.get(
  '/workdir-policy',
  authModule.requireAuth,
  (_req: Request, res: Response) => {
    const s = snapshot();
    res.json({ ok: true, allow: s.allow });   // 故意不返 deny
  },
);
```

2. `backend/src/broker/cli.ts` 第 318-321 行附近的 `workdirPolicy:() => ({ allow: ... })` 改为同时返回 deny:

```ts
workdirPolicy: () => ({
  allow: ((currentUserConfig.workdirAllow as string[] | undefined) ?? []),
  deny: ((currentUserConfig.workdirDeny as string[] | undefined) ?? []),
}),
```

3. 跑现有 workdir-policy 端点单测确认契约未变:

```bash
pnpm --filter auvezy-terminal-remote exec vitest run src/api/workdir-policy-routes.test.ts || true
```

如果该测试不存在,跳过本检查。`grep -l workdir-policy-routes backend/src/api/*.test.ts` 验证有无。

- [ ] **Step 3: 重写 `file-routes.ts` 完整版**

```ts
/**
 * /api/files/* 路由(broker 端,MVP 不含 search;search 阶段 4 单独挂)
 *
 * 鉴权:authModule.requireAuth
 * 错误:统一 FileError;/raw 用 X-ATR-Error header 不返 JSON
 */

import { Router, type Request, type Response, type NextFunction } from 'express';
import { createReadStream } from 'node:fs';
import { lstat } from 'node:fs/promises';
import {
  ErrorCode,
  FILE_RAW_MAX_BYTES,
  type FileListResponse,
  type FileReadResponse,
  type FileStatResponse,
} from 'auvezy-terminal-remote-shared';
import type { InstanceRegistryManager } from '../registry/instance-registry.js';
import type { AuthModule } from '../auth/auth-middleware.js';
import type { WorkdirPolicySnapshot } from './workdir-policy-routes.js';
import { FileError, AppError } from '../errors.js';
import { logger } from '../logger/logger.js';
import { resolveSafePath, type WorkdirPolicy } from '../files/path-resolver.js';
import { listDir } from '../files/list-dir.js';
import { detectMime, detectLang } from '../files/mime-detect.js';
import { readTextFile } from '../files/read-file.js';

export interface FileRoutesOptions {
  authModule: AuthModule;
  registry: InstanceRegistryManager;
  /** 工厂:返回当前生效的完整 policy(含 deny;允许 file-routes 内部复用) */
  workdirPolicy: () => WorkdirPolicySnapshot;
}

export function createFileRoutes(opts: FileRoutesOptions): Router {
  const router = Router();
  const { authModule, registry, workdirPolicy } = opts;

  router.get('/files/list', authModule.requireAuth, wrap(async (req, res) => {
    const { cwd, policy } = await resolveContext(req, registry, workdirPolicy);
    const target = resolveSafePath(cwd, asString(req.query.path), policy);
    const entries = await listDir(target);
    const parent = computeParent(target, policy);
    const payload: FileListResponse = {
      ok: true, cwd, path: target, parent, entries,
    };
    res.json(payload);
  }));

  router.get('/files/stat', authModule.requireAuth, wrap(async (req, res) => {
    const { cwd, policy } = await resolveContext(req, registry, workdirPolicy);
    const target = resolveSafePath(cwd, asString(req.query.path), policy);
    const st = await lstat(target);
    const kind = st.isSymbolicLink() ? 'symlink'
      : st.isDirectory() ? 'dir'
      : st.isFile() ? 'file' : 'other';
    const payload: FileStatResponse = {
      ok: true, path: target, kind,
      size: kind === 'dir' ? 0 : st.size,
      mtimeMs: st.mtimeMs,
    };
    if (kind === 'file') {
      const m = detectMime(target);
      payload.mime = m.mime;
      payload.previewable = m.previewable;
    }
    res.json(payload);
  }));

  router.get('/files/read', authModule.requireAuth, wrap(async (req, res) => {
    const { cwd, policy } = await resolveContext(req, registry, workdirPolicy);
    const target = resolveSafePath(cwd, asString(req.query.path), policy);
    const st = await lstat(target);
    if (!st.isFile()) {
      throw new FileError(ErrorCode.FILE_TYPE_FORBID, `not a regular file`, 409);
    }
    const r = await readTextFile(target);
    const m = detectMime(target);
    const payload: FileReadResponse = {
      ok: true, path: target, mime: m.mime,
      content: r.content, truncated: r.truncated, size: r.size,
      lang: detectLang(target),
    };
    res.json(payload);
  }));

  // /raw 不走 wrap,自己处理错误(不返 JSON)
  router.get('/files/raw', authModule.requireAuth, async (req, res) => {
    try {
      const { cwd, policy } = await resolveContext(req, registry, workdirPolicy);
      const target = resolveSafePath(cwd, asString(req.query.path), policy);
      const st = await lstat(target);
      if (!st.isFile()) throw new FileError(ErrorCode.FILE_TYPE_FORBID, '', 409);
      if (st.size > FILE_RAW_MAX_BYTES) {
        throw new FileError(ErrorCode.FILE_TOO_LARGE, '', 413);
      }
      const { mime } = detectMime(target);
      res.setHeader('Content-Type', mime);
      res.setHeader('Cache-Control', 'private, max-age=0');
      res.setHeader('Content-Length', String(st.size));
      createReadStream(target).pipe(res);
    } catch (err) {
      const code = err instanceof AppError ? err.code : ErrorCode.INTERNAL_ERROR;
      const status = err instanceof AppError ? err.httpStatus : 500;
      res.setHeader('X-ATR-Error', String(code));
      res.status(status).end();
    }
  });

  return router;
}

// ──────────────── 内部辅助 ────────────────

type AsyncHandler = (req: Request, res: Response) => Promise<void>;
function wrap(h: AsyncHandler) {
  return async (req: Request, res: Response, next: NextFunction): Promise<void> => {
    try { await h(req, res); }
    catch (err) {
      if (err instanceof AppError) {
        res.status(err.httpStatus).json({ error: { code: err.code, message: err.message } });
        return;
      }
      logger.error({ err }, '/api/files unexpected error');
      res.status(500).json({ error: { code: ErrorCode.INTERNAL_ERROR, message: 'internal error' } });
    }
  };
}

function asString(v: unknown): string | undefined {
  return typeof v === 'string' ? v : undefined;
}

async function resolveContext(
  req: Request,
  registry: InstanceRegistryManager,
  workdirPolicy: () => WorkdirPolicySnapshot,
): Promise<{ cwd: string; policy: WorkdirPolicy }> {
  const instanceId = req.query.instanceId;
  if (typeof instanceId !== 'string' || instanceId.length === 0) {
    throw new FileError(ErrorCode.BAD_REQUEST, 'instanceId is required', 400);
  }
  const all = await registry.list();
  const inst = all.find((i) => i.instanceId === instanceId);
  if (!inst) {
    throw new FileError(ErrorCode.INSTANCE_NOT_FOUND, `instance not found: ${instanceId}`, 404);
  }
  const snap = workdirPolicy();
  return { cwd: inst.cwd, policy: { allow: snap.allow, deny: snap.deny } };
}

/** 计算 parent(cwd 的物理父目录;过 policy 闸,失败 → null) */
function computeParent(current: string, policy: WorkdirPolicy): string | null {
  const parentPath = current.replace(/[\\/][^\\/]+[\\/]?$/, '') || '/';
  if (parentPath === current) return null;
  try {
    resolveSafePath(parentPath, '.', policy);  // 用 resolveSafePath 跑一次 policy
    return parentPath;
  } catch {
    return null;
  }
}
```

- [ ] **Step 4: 跑测试**

```bash
pnpm --filter auvezy-terminal-remote exec vitest run src/api/file-routes.test.ts
```

Expected: PASS(7/7)。

- [ ] **Step 5: commit**

```bash
git add backend/src/api/file-routes.ts \
        backend/src/api/workdir-policy-routes.ts \
        backend/src/broker/cli.ts
git commit -m "feat(file-browser): 阶段 3.5 — /api/files list/read/stat/raw 实现

- WorkdirPolicySnapshot 加 deny 字段(broker 内部复用,/api/workdir-policy 端点仍只返 allow)
- file-routes 4 端点 + wrap helper + /raw 用 X-ATR-Error header
- computeParent:用 resolveSafePath 校验上级是否还在 policy 内"
```

---

### Task 3.5b — 限流 + 审计日志(补 design §5.6 / §5.7)

**Files:**
- Modify: `backend/src/api/file-routes.ts`

- [ ] **Step 1: 在 `createFileRoutes` 内,函数顶部创建两个 RateLimiter 实例**

```ts
import { RateLimiter } from '../auth/rate-limiter.js';

// (在 createFileRoutes 内,Router 创建之后)
const fileLimiter = new RateLimiter(120);     // /list /read /stat /raw 共享 — 每 IP 120/min
const searchLimiter = new RateLimiter(20);    // /search 独立 — 每 IP 20/min
```

- [ ] **Step 2: 加一个 middleware helper**

```ts
function requireRate(limiter: RateLimiter): (req: Request, res: Response, next: NextFunction) => void {
  return (req, res, next) => {
    const ip = req.ip ?? 'unknown';
    if (!limiter.attempt(ip)) {
      res.status(429).json({
        error: { code: ErrorCode.AUTH_RATE_LIMITED, message: 'rate limited' },
      });
      return;
    }
    next();
  };
}
```

- [ ] **Step 3: 把 fileLimiter 挂到 list/stat/read/raw,searchLimiter 挂到 search**

```ts
// 把已有的
//   router.get('/files/list', authModule.requireAuth, wrap(...))
// 改为
//   router.get('/files/list', authModule.requireAuth, requireRate(fileLimiter), wrap(...))
// 对 /stat /read /raw 同样改;/search 同样接 requireRate(searchLimiter)
```

- [ ] **Step 4: 加审计日志(每个 handler 末尾 success path 之前)**

在每个 handler 的 `res.json(...)` / `res.end()` 前一行加:

```ts
logger.info({
  action: '<list|stat|read|raw|search>',  // 各 handler 不同
  instanceId, path: target,                 // search 路径用 scopePath
  ip: req.ip,
  elapsedMs: Date.now() - tStart,           // tStart 在 handler 顶部 const tStart = Date.now()
}, '/api/files audit');
```

`instanceId` 取自 query;为了避免重复读,可以把 `resolveContext` 改成同时返回 `instanceId` 字段:

```ts
async function resolveContext(req: Request, ...): Promise<{ cwd: string; policy: WorkdirPolicy; instanceId: string }> {
  // ...
  return { cwd: inst.cwd, policy: { allow: snap.allow, deny: snap.deny }, instanceId };
}
```

handler 内取 `const { cwd, policy, instanceId } = await resolveContext(...)`。

- [ ] **Step 5: 在 file-routes.test.ts 加 2 个限流测试**

在 describe 内末尾追加:

```ts
it('list 超 120/min 返 429', async () => {
  const app = makeApp(root);
  // 现有 RateLimiter 实例是 createFileRoutes 内创建,各 makeApp 独立 → 同一 app 内连发
  let last = 0;
  for (let i = 0; i < 125; i++) {
    const res = await request(app).get(`/api/files/list?instanceId=${FAKE_INSTANCE_ID}`);
    last = res.status;
  }
  // 不严格断 == 429,只确认最后一次被限了
  expect(last).toBe(429);
}, 15_000);
```

- [ ] **Step 6: 跑测试 + commit**

```bash
pnpm --filter auvezy-terminal-remote exec vitest run src/api/file-routes.test.ts
git add backend/src/api/file-routes.ts backend/src/api/file-routes.test.ts
git commit -m "feat(file-browser): 阶段 3.5b — per-IP 限流(120/20 per min) + 审计日志

复用现有 RateLimiter(per-IP 滑动窗口);超限返 429 AUTH_RATE_LIMITED。
每次请求落 logger.info(action / instanceId / path / ip / elapsedMs),
broker daily rotate log 自动归档。"
```

---

### Task 3.6 — 把 `createFileRoutes` 挂到 `createBrokerApiRouter`

**Files:**
- Modify: `backend/src/api/router.ts`

- [ ] **Step 1: 在 `createBrokerApiRouter` 末尾 `return router;` 之前加**

```ts
import { createFileRoutes } from './file-routes.js';
// (在 imports 区)

// 在 createBrokerApiRouter 函数体最末尾、return 之前:
router.use(
  createFileRoutes({
    authModule: opts.authModule,
    registry: opts.registry,
    workdirPolicy: opts.workdirPolicy,
  }),
);
```

- [ ] **Step 2: typecheck + 全量测试**

```bash
pnpm --filter auvezy-terminal-remote typecheck
pnpm --filter auvezy-terminal-remote test
```

Expected: 全绿。

- [ ] **Step 3: commit**

```bash
git add backend/src/api/router.ts
git commit -m "feat(file-browser): 阶段 3.6 — 把 file-routes 挂入 createBrokerApiRouter"
```

---

### Task 3.7 — 阶段 3 进度文档

**Files:**
- Create: `docs/plans/file-browser/progress/03-阶段3-非搜索路由.md`

- [ ] **Step 1: 写**

```markdown
# 阶段 3 — read-file + 非搜索路由

> 状态:✅ 已完成(<日期>)

## 完成项

- 3.1 / 3.2 — readTextFile 三段闸(NUL/截断/replacement char)
- 3.3 — WorkdirPolicySnapshot 加内部 deny(端点契约不变)
- 3.4 / 3.5 — /api/files list/stat/read/raw 4 端点 + 7 个 supertest
- 3.5b — per-IP 限流(120/min)+ 审计日志(action/path/ip/elapsedMs)
- 3.6 — 挂入 createBrokerApiRouter

## 已知边界

- /raw 错误不返 JSON,用 X-ATR-Error header
- /read 非 regular file → 409 FILE_TYPE_FORBID
- parent 越界则返 null,前端禁用"上级"按钮
```

- [ ] **Step 2: commit**

```bash
git add docs/plans/file-browser/progress/03-阶段3-非搜索路由.md
git commit -m "docs(file-browser): 阶段 3 进度文档"
```

---

## 阶段 4 — search-engine + SSE 路由

阶段产物:`/api/files/search` 流式工作,name + content 双模式 + ReDoS 防护。

### Task 4.1 — `search-engine.ts` 写测试

**Files:**
- Create: `backend/src/files/search-engine.test.ts`

- [ ] **Step 1: 写测试**

```ts
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import { mkdtempSync, mkdirSync, writeFileSync, rmSync } from 'node:fs';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { runSearch, type SearchHit } from './search-engine.js';

describe('runSearch', () => {
  let root: string;
  beforeAll(() => {
    root = mkdtempSync(join(tmpdir(), 'fb-search-'));
    mkdirSync(join(root, 'node_modules'));
    mkdirSync(join(root, 'src'));
    writeFileSync(join(root, 'README.md'), 'hello checkWorkdir world\nsecond line');
    writeFileSync(join(root, 'src', 'main.ts'), 'export const checkWorkdir = 1;\n// other');
    writeFileSync(join(root, 'node_modules', 'leak.txt'), 'checkWorkdir but ignored');
    writeFileSync(join(root, 'bin.dat'), Buffer.from([0x00, 0x01, 0x02]));
  });
  afterAll(() => rmSync(root, { recursive: true, force: true }));

  it('name 模式按 substring 匹配', async () => {
    const hits: SearchHit[] = [];
    const done = await runSearch({
      scope: root,
      q: 'main',
      mode: 'name',
      caseSensitive: false,
      regex: false,
      policy: { allow: [], deny: [] },
      emit: (h) => hits.push(h),
    });
    expect(hits.some((h) => h.kind === 'name' && h.path.endsWith('main.ts'))).toBe(true);
    expect(done.scanned).toBeGreaterThan(0);
  });

  it('content 模式命中行号正确', async () => {
    const hits: SearchHit[] = [];
    await runSearch({
      scope: root, q: 'checkWorkdir', mode: 'content',
      caseSensitive: true, regex: false,
      policy: { allow: [], deny: [] },
      emit: (h) => hits.push(h),
    });
    const ts = hits.find((h) => h.kind === 'content' && h.path.endsWith('main.ts'));
    expect(ts).toBeTruthy();
    expect((ts as { line: number }).line).toBe(1);
  });

  it('忽略 node_modules', async () => {
    const hits: SearchHit[] = [];
    await runSearch({
      scope: root, q: 'checkWorkdir', mode: 'content',
      caseSensitive: true, regex: false,
      policy: { allow: [], deny: [] },
      emit: (h) => hits.push(h),
    });
    expect(hits.every((h) => !h.path.includes('node_modules'))).toBe(true);
  });

  it('二进制文件跳过', async () => {
    const hits: SearchHit[] = [];
    await runSearch({
      scope: root, q: '', mode: 'content',
      caseSensitive: true, regex: false,
      policy: { allow: [], deny: [] },
      emit: (h) => hits.push(h),
    });
    expect(hits.every((h) => !h.path.endsWith('bin.dat'))).toBe(true);
  });

  it('regex 含 \\n 抛 SEARCH_INVALID_Q', async () => {
    await expect(
      runSearch({
        scope: root, q: 'a\nb', mode: 'content',
        caseSensitive: true, regex: true,
        policy: { allow: [], deny: [] },
        emit: () => {},
      }),
    ).rejects.toThrow(/SEARCH_INVALID_Q|cross-line|invalid/i);
  });

  it('畸形 regex 抛 SEARCH_INVALID_Q', async () => {
    await expect(
      runSearch({
        scope: root, q: '(', mode: 'content',
        caseSensitive: true, regex: true,
        policy: { allow: [], deny: [] },
        emit: () => {},
      }),
    ).rejects.toThrow(/SEARCH_INVALID_Q|invalid/i);
  });
});
```

- [ ] **Step 2: 跑失败 + commit 测试**

```bash
pnpm --filter auvezy-terminal-remote exec vitest run src/files/search-engine.test.ts
git add backend/src/files/search-engine.test.ts
git commit -m "test(file-browser): 阶段 4.1 — search-engine 失败测试"
```

---

### Task 4.2 — `search-engine.ts` 实现

**Files:**
- Create: `backend/src/files/search-engine.ts`

- [ ] **Step 1: 实现**

```ts
/**
 * search-engine:文件名 + 内容搜索
 *
 * 设计要点(design §5.5):
 *  - 递归遍历 scope 目录,深度 ≤ 6,单请求扫描 entry 数 ≤ 5000
 *  - 忽略目录硬编码列表(node_modules / dist / .git ...)
 *  - 二进制文件跳过(前 4 KiB 含 NUL)
 *  - 单文件总扫描 100 ms 硬超时,全请求 5 s
 *  - 并发文件数 ≤ 8
 *  - regex 含 \n / 编译失败 → SEARCH_INVALID_Q
 */

import { opendir, open, stat } from 'node:fs/promises';
import { createReadStream } from 'node:fs';
import { createInterface } from 'node:readline';
import { join } from 'node:path';
import {
  ErrorCode,
  SEARCH_FILE_TIMEOUT_MS,
  SEARCH_TOTAL_TIMEOUT_MS,
  SEARCH_CONCURRENCY,
  SEARCH_MAX_NAME_RESULTS,
  SEARCH_MAX_CONTENT_RESULTS,
  type SearchEvent,
} from 'auvezy-terminal-remote-shared';
import { FileError } from '../errors.js';
import { checkWorkdir } from '../utils/workdir-policy.js';

const IGNORE_DIRS = new Set([
  'node_modules', '.git', '.svn', '.hg',
  'dist', 'build', '.next', '.turbo', '.cache', 'target',
  '.venv', '__pycache__', '.DS_Store',
]);
const MAX_DEPTH = 6;
const MAX_ENTRIES_SCANNED = 5000;
const NUL_PROBE_BYTES = 4 * 1024;
const PREVIEW_MAX_CHARS = 200;
const FILE_MAX_SCAN_BYTES = 2 * 1024 * 1024;

export type SearchHit = SearchEvent;

export interface SearchOptions {
  scope: string;
  q: string;
  mode: 'name' | 'content' | 'both';
  caseSensitive: boolean;
  regex: boolean;
  policy: { allow: readonly string[]; deny: readonly string[] };
  emit: (hit: SearchHit) => void;
  /** 提前取消(请求 close) */
  cancelSignal?: AbortSignal;
}

export interface SearchSummary {
  truncated: boolean;
  scanned: number;
  elapsedMs: number;
}

export async function runSearch(opts: SearchOptions): Promise<SearchSummary> {
  const start = Date.now();
  let scanned = 0;
  let nameHits = 0;
  let contentHits = 0;
  let truncated = false;

  // 编译 matcher
  let nameMatch: (name: string) => boolean;
  let contentMatch: (line: string) => { start: number; end: number } | null;
  if (opts.regex) {
    if (opts.q.includes('\n')) {
      throw new FileError(ErrorCode.SEARCH_INVALID_Q, 'cross-line regex disallowed', 400);
    }
    let re: RegExp;
    try {
      re = new RegExp(opts.q, opts.caseSensitive ? '' : 'i');
    } catch (err) {
      throw new FileError(ErrorCode.SEARCH_INVALID_Q, `invalid regex: ${String(err)}`, 400);
    }
    nameMatch = (n) => re.test(n);
    contentMatch = (line) => {
      const m = re.exec(line);
      re.lastIndex = 0;
      return m ? { start: m.index, end: m.index + m[0].length } : null;
    };
  } else {
    const needle = opts.caseSensitive ? opts.q : opts.q.toLowerCase();
    nameMatch = (n) => (opts.caseSensitive ? n : n.toLowerCase()).includes(needle);
    contentMatch = (line) => {
      const hay = opts.caseSensitive ? line : line.toLowerCase();
      const idx = hay.indexOf(needle);
      return idx === -1 ? null : { start: idx, end: idx + needle.length };
    };
  }

  // 收集候选文件(name 命中 + 待 content 扫描)
  const candidates: string[] = [];

  async function walk(dir: string, depth: number): Promise<void> {
    if (depth > MAX_DEPTH) return;
    if (scanned >= MAX_ENTRIES_SCANNED) { truncated = true; return; }
    if (Date.now() - start > SEARCH_TOTAL_TIMEOUT_MS) { truncated = true; return; }
    if (opts.cancelSignal?.aborted) return;

    let dirh;
    try { dirh = await opendir(dir); } catch { return; }

    for await (const ent of dirh) {
      scanned++;
      if (scanned >= MAX_ENTRIES_SCANNED) { truncated = true; break; }
      if (Date.now() - start > SEARCH_TOTAL_TIMEOUT_MS) { truncated = true; break; }
      if (opts.cancelSignal?.aborted) break;

      const full = join(dir, ent.name);

      if (ent.isDirectory()) {
        if (IGNORE_DIRS.has(ent.name)) continue;
        // policy 闸:子目录命中 deny 整段跳
        if (checkWorkdir(full, opts.policy.allow, opts.policy.deny) !== null) continue;
        await walk(full, depth + 1);
        continue;
      }

      if (!ent.isFile()) continue;

      // name 模式
      if (opts.mode !== 'content' && nameMatch(ent.name) && nameHits < SEARCH_MAX_NAME_RESULTS) {
        try {
          const st = await stat(full);
          opts.emit({ kind: 'name', path: full, size: st.size, mtimeMs: st.mtimeMs });
          nameHits++;
        } catch { /* ignore */ }
      }

      if (opts.mode !== 'name') candidates.push(full);
    }
  }

  await walk(opts.scope, 0);

  // content 扫描:concurrency 8
  if (opts.mode !== 'name' && candidates.length > 0) {
    const queue = candidates.slice();
    const workers: Promise<void>[] = [];
    for (let i = 0; i < SEARCH_CONCURRENCY; i++) {
      workers.push((async () => {
        while (queue.length > 0) {
          if (contentHits >= SEARCH_MAX_CONTENT_RESULTS) { truncated = true; return; }
          if (Date.now() - start > SEARCH_TOTAL_TIMEOUT_MS) { truncated = true; return; }
          if (opts.cancelSignal?.aborted) return;
          const f = queue.shift();
          if (!f) return;
          await scanFile(f);
        }
      })());
    }
    await Promise.all(workers);
  }

  async function scanFile(path: string): Promise<void> {
    const fileStart = Date.now();
    let st;
    try { st = await stat(path); } catch { return; }
    if (st.size > FILE_MAX_SCAN_BYTES) return;

    // 二进制识别
    try {
      const fh = await open(path, 'r');
      try {
        const probeLen = Math.min(NUL_PROBE_BYTES, st.size);
        if (probeLen > 0) {
          const buf = Buffer.alloc(probeLen);
          await fh.read(buf, 0, probeLen, 0);
          if (buf.includes(0x00)) return;
        }
      } finally { await fh.close(); }
    } catch { return; }

    // 流式按行
    await new Promise<void>((resolveP) => {
      const stream = createReadStream(path, { encoding: 'utf-8' });
      const rl = createInterface({ input: stream, crlfDelay: Infinity });
      let lineNo = 0;
      let stopped = false;
      const stop = (): void => {
        if (stopped) return;
        stopped = true;
        rl.close();
        stream.destroy();
        resolveP();
      };
      rl.on('line', (line) => {
        lineNo++;
        if (Date.now() - fileStart > SEARCH_FILE_TIMEOUT_MS) { stop(); return; }
        if (contentHits >= SEARCH_MAX_CONTENT_RESULTS) { truncated = true; stop(); return; }
        if (opts.cancelSignal?.aborted) { stop(); return; }
        const m = contentMatch(line);
        if (m) {
          const preview = line.length > PREVIEW_MAX_CHARS
            ? line.slice(0, PREVIEW_MAX_CHARS)
            : line;
          opts.emit({
            kind: 'content', path, line: lineNo, preview,
            matchStart: Math.min(m.start, preview.length),
            matchEnd: Math.min(m.end, preview.length),
          });
          contentHits++;
        }
      });
      rl.on('close', () => resolveP());
      rl.on('error', () => stop());
      stream.on('error', () => stop());
    });
  }

  return {
    truncated,
    scanned,
    elapsedMs: Date.now() - start,
  };
}
```

- [ ] **Step 2: 跑测试**

```bash
pnpm --filter auvezy-terminal-remote exec vitest run src/files/search-engine.test.ts
```

Expected: PASS(6/6)。

- [ ] **Step 3: commit**

```bash
git add backend/src/files/search-engine.ts
git commit -m "feat(file-browser): 阶段 4.2 — search-engine 实现

name+content 双模式 / 8 并发 / 100ms 单文件 / 5s 全请求超时 /
跨行 regex 拒 / 畸形 regex 拒 / 二进制跳 / IGNORE_DIRS 硬编码列表。"
```

---

### Task 4.3 — search 端点写测试

**Files:**
- Modify: `backend/src/api/file-routes.test.ts`(在 describe 末尾追加 SSE 测试块)

- [ ] **Step 1: 在测试文件末尾追加**

```ts
describe('GET /api/files/search SSE', () => {
  it('q 缺失 → 400', async () => {
    const app = makeApp(root);
    const res = await request(app).get(`/api/files/search?instanceId=${FAKE_INSTANCE_ID}`);
    expect(res.status).toBe(400);
  });

  it('name 模式命中流式返回', async () => {
    const app = makeApp(root);
    const res = await request(app)
      .get(`/api/files/search?instanceId=${FAKE_INSTANCE_ID}&q=README&mode=name`)
      .buffer(true).parse((res, cb) => {
        let data = '';
        res.on('data', (c) => { data += c; });
        res.on('end', () => cb(null, data));
      });
    expect(res.status).toBe(200);
    expect(res.headers['content-type']).toMatch(/event-stream/);
    expect(res.body).toContain('"kind":"name"');
    expect(res.body).toContain('README.md');
    expect(res.body).toContain('event: done');
  });
});
```

- [ ] **Step 2: 跑失败 + commit**

```bash
pnpm --filter auvezy-terminal-remote exec vitest run src/api/file-routes.test.ts
git add backend/src/api/file-routes.test.ts
git commit -m "test(file-browser): 阶段 4.3 — search SSE 失败测试"
```

Expected: FAIL — /search 路径未实现。

---

### Task 4.4 — search 端点实现

**Files:**
- Modify: `backend/src/api/file-routes.ts`(在 raw 后追加 search 路由)

- [ ] **Step 1: 在 router 上挂 /files/search**

在 `createFileRoutes` 内 `router.get('/files/raw', ...)` 之后追加:

```ts
router.get('/files/search', authModule.requireAuth, async (req, res) => {
  // 参数校验
  const q = asString(req.query.q);
  if (!q || q.length === 0 || q.length > SEARCH_MAX_Q_LENGTH) {
    res.status(400).json({ error: { code: ErrorCode.BAD_REQUEST, message: 'q is required (1..200)' } });
    return;
  }
  const modeRaw = asString(req.query.mode) ?? 'name';
  if (modeRaw !== 'name' && modeRaw !== 'content' && modeRaw !== 'both') {
    res.status(400).json({ error: { code: ErrorCode.BAD_REQUEST, message: 'invalid mode' } });
    return;
  }
  let ctx;
  try {
    ctx = await resolveContext(req, registry, workdirPolicy);
  } catch (err) {
    if (err instanceof AppError) {
      res.status(err.httpStatus).json({ error: { code: err.code, message: err.message } });
    } else { res.status(500).end(); }
    return;
  }
  const scope = asString(req.query.scope);
  let scopePath: string;
  try {
    scopePath = resolveSafePath(ctx.cwd, scope, ctx.policy);
    const st = await lstat(scopePath);
    if (!st.isDirectory()) {
      throw new FileError(ErrorCode.BAD_REQUEST, 'scope must be a directory', 400);
    }
  } catch (err) {
    if (err instanceof AppError) {
      res.status(err.httpStatus).json({ error: { code: err.code, message: err.message } });
    } else { res.status(500).end(); }
    return;
  }

  // SSE 头
  res.setHeader('Content-Type', 'text/event-stream');
  res.setHeader('Cache-Control', 'no-cache, no-transform');
  res.setHeader('Connection', 'keep-alive');
  res.setHeader('X-Accel-Buffering', 'no');
  res.flushHeaders();

  const ac = new AbortController();
  req.on('close', () => ac.abort());

  try {
    const summary = await runSearch({
      scope: scopePath,
      q,
      mode: modeRaw,
      caseSensitive: asString(req.query.caseSensitive) === '1',
      regex: asString(req.query.regex) === '1',
      policy: ctx.policy,
      cancelSignal: ac.signal,
      emit: (hit) => {
        res.write(`event: match\ndata: ${JSON.stringify(hit)}\n\n`);
      },
    });
    res.write(`event: done\ndata: ${JSON.stringify(summary)}\n\n`);
  } catch (err) {
    const code = err instanceof AppError ? err.code : ErrorCode.INTERNAL_ERROR;
    res.write(`event: error\ndata: ${JSON.stringify({ code, message: String(err) })}\n\n`);
  } finally {
    res.end();
  }
});
```

并在文件顶部 imports 加:

```ts
import { runSearch } from '../files/search-engine.js';
import { SEARCH_MAX_Q_LENGTH } from 'auvezy-terminal-remote-shared';
```

- [ ] **Step 2: 跑测试**

```bash
pnpm --filter auvezy-terminal-remote exec vitest run src/api/file-routes.test.ts
```

Expected: PASS(全部含新增 SSE 用例)。

- [ ] **Step 3: commit**

```bash
git add backend/src/api/file-routes.ts
git commit -m "feat(file-browser): 阶段 4.4 — /api/files/search SSE 端点

参数校验(q 1..200 / mode 枚举 / scope 必须为目录) + AbortController
绑定 req.close + match/done/error 三种 SSE event。"
```

---

### Task 4.5 — 阶段 4 进度文档

**Files:**
- Create: `docs/plans/file-browser/progress/04-阶段4-搜索.md`

- [ ] **Step 1: 写**

```markdown
# 阶段 4 — 文件搜索

> 状态:✅ 已完成(<日期>)

## 完成项

- 4.1 / 4.2 — search-engine.ts(name + content 双模式 / 8 并发 / 100ms + 5s 超时 / IGNORE_DIRS / 二进制跳过 / ReDoS 防护)
- 4.3 / 4.4 — /api/files/search SSE 端点(参数校验 + AbortController + match/done/error)

## 后端阶段完结

阶段 1-4 完成后,所有 `/api/files/*` 端点可独立工作。下一步前端集成。
```

- [ ] **Step 2: commit**

```bash
git add docs/plans/file-browser/progress/04-阶段4-搜索.md
git commit -m "docs(file-browser): 阶段 4 进度文档"
```

---

## 阶段 5 — 前端 FileBrowserSheet(无高亮)+ useFiles + i18n

阶段产物:前端能列目录 + 文本预览(无高亮)+ 图片预览,无搜索,无高亮。

### Task 5.1 — `services/files-api.ts` 客户端

**Files:**
- Create: `frontend/src/services/files-api.ts`

- [ ] **Step 1: 实现**

```ts
/**
 * /api/files/* 客户端封装
 *
 * 所有调用走相对路径(broker 反代回 HTML 已注入 <base href>)。
 */

import type {
  FileListResponse, FileReadResponse, FileStatResponse,
  SearchEvent, SearchDone,
} from 'auvezy-terminal-remote-shared';

export async function listFiles(instanceId: string, path?: string): Promise<FileListResponse> {
  const q = new URLSearchParams({ instanceId });
  if (path) q.set('path', path);
  const r = await fetch(`api/files/list?${q.toString()}`, { credentials: 'include' });
  if (!r.ok) throw await asError(r);
  return r.json();
}

export async function readFile(instanceId: string, path: string): Promise<FileReadResponse> {
  const q = new URLSearchParams({ instanceId, path });
  const r = await fetch(`api/files/read?${q.toString()}`, { credentials: 'include' });
  if (!r.ok) throw await asError(r);
  return r.json();
}

export async function statFile(instanceId: string, path: string): Promise<FileStatResponse> {
  const q = new URLSearchParams({ instanceId, path });
  const r = await fetch(`api/files/stat?${q.toString()}`, { credentials: 'include' });
  if (!r.ok) throw await asError(r);
  return r.json();
}

export function rawUrl(instanceId: string, path: string): string {
  const q = new URLSearchParams({ instanceId, path });
  return `api/files/raw?${q.toString()}`;
}

export interface SearchHandle {
  cancel(): void;
}

export function streamSearch(
  instanceId: string,
  params: { q: string; mode: 'name'|'content'|'both'; scope?: string; caseSensitive?: boolean; regex?: boolean },
  onMatch: (m: SearchEvent) => void,
  onDone: (d: SearchDone) => void,
  onError: (code: string) => void,
): SearchHandle {
  const sp = new URLSearchParams({
    instanceId, q: params.q, mode: params.mode,
    caseSensitive: params.caseSensitive ? '1' : '0',
    regex: params.regex ? '1' : '0',
  });
  if (params.scope) sp.set('scope', params.scope);
  const es = new EventSource(`api/files/search?${sp.toString()}`, { withCredentials: true });
  es.addEventListener('match', (ev) => onMatch(JSON.parse((ev as MessageEvent).data)));
  es.addEventListener('done', (ev) => { onDone(JSON.parse((ev as MessageEvent).data)); es.close(); });
  es.addEventListener('error', (ev) => {
    const data = (ev as MessageEvent).data;
    onError(typeof data === 'string' ? data : 'STREAM_ERROR');
    es.close();
  });
  return { cancel: () => es.close() };
}

async function asError(r: Response): Promise<Error> {
  try {
    const body = await r.json();
    const code = body?.error?.code ?? `HTTP_${r.status}`;
    const err = new Error(body?.error?.message ?? `request failed: ${r.status}`);
    (err as Error & { code: string }).code = code;
    return err;
  } catch {
    return new Error(`request failed: ${r.status}`);
  }
}
```

- [ ] **Step 2: typecheck**

```bash
pnpm --filter auvezy-terminal-remote-frontend typecheck
```

- [ ] **Step 3: commit**

```bash
git add frontend/src/services/files-api.ts
git commit -m "feat(file-browser): 阶段 5.1 — files-api 客户端封装(含 SSE 搜索)"
```

---

### Task 5.2 — `useFiles` hook

**Files:**
- Create: `frontend/src/hooks/useFiles.ts`

- [ ] **Step 1: 实现**

```ts
/**
 * useFiles(instanceId)
 *
 * 暴露 list / read / stat 三个方法,内部对 list 做 path 缓存(SWR-like)。
 * 切实例时整个 hook 重置(useMemo by instanceId)。
 */

import { useMemo, useState, useCallback } from 'react';
import type { FileListResponse, FileReadResponse, FileStatResponse } from 'auvezy-terminal-remote-shared';
import { listFiles, readFile, statFile } from '../services/files-api.js';

export interface UseFiles {
  list(path?: string): Promise<FileListResponse>;
  read(path: string): Promise<FileReadResponse>;
  stat(path: string): Promise<FileStatResponse>;
  /** 直接读缓存,无网络;miss = undefined */
  cachedList(path: string): FileListResponse | undefined;
}

export function useFiles(instanceId: string | null): UseFiles {
  const [cache, setCache] = useState<Map<string, FileListResponse>>(new Map());

  // 切实例 → 清缓存
  useMemo(() => { setCache(new Map()); }, [instanceId]);

  const list = useCallback(async (path?: string) => {
    if (!instanceId) throw new Error('no active instance');
    const r = await listFiles(instanceId, path);
    setCache((prev) => {
      const next = new Map(prev);
      next.set(r.path, r);
      return next;
    });
    return r;
  }, [instanceId]);

  const read = useCallback(async (path: string) => {
    if (!instanceId) throw new Error('no active instance');
    return readFile(instanceId, path);
  }, [instanceId]);

  const stat = useCallback(async (path: string) => {
    if (!instanceId) throw new Error('no active instance');
    return statFile(instanceId, path);
  }, [instanceId]);

  const cachedList = useCallback((path: string) => cache.get(path), [cache]);

  return { list, read, stat, cachedList };
}
```

- [ ] **Step 2: typecheck + commit**

```bash
pnpm --filter auvezy-terminal-remote-frontend typecheck
git add frontend/src/hooks/useFiles.ts
git commit -m "feat(file-browser): 阶段 5.2 — useFiles hook(list 路径缓存 + 切实例重置)"
```

---

### Task 5.3 — i18n 文案

**Files:**
- Modify: `frontend/src/i18n/messages.ts`(zh-CN + en-US)

- [ ] **Step 1: 找 messages.ts 结构**

```bash
grep -n "share\\." /mnt/d/github/open-terminal-remote/frontend/src/i18n/messages.ts | head -10
```

参照 `share.*` 键空间,在同位置加 `files.*`。

- [ ] **Step 2: 加 files 键**(中英对照)

```ts
// 在 zh-CN 对象内追加:
'files.title': '文件浏览',
'files.breadcrumbRoot': '工作目录',
'files.toolbar.up': '上级',
'files.toolbar.refresh': '刷新',
'files.toolbar.showHidden': '显示隐藏',
'files.toolbar.cwd': '回到工作目录',
'files.empty': '该目录为空',
'files.search.placeholder': '搜索文件名或内容…',
'files.search.scanning': '已扫 {scanned} 文件 · {hits} 个命中…',
'files.search.truncated': '已达上限,缩窄关键字',
'files.search.empty': '无匹配',
'files.preview.binary': '该文件无法预览',
'files.preview.truncated': '内容已截断到 2 MiB',
'files.preview.highlightOff': '文件过大,已禁用高亮',
'files.preview.imageTooLarge': '图片超过 8 MiB,无法预览',
'files.error.pathNotFound': '路径不存在',
'files.error.pathForbidden': '路径不在允许范围内',
'files.error.fileBinary': '该文件是二进制,无法以文本预览',
'files.error.invalidQuery': '搜索语法错误',

// 在 en-US 对象内追加对应英文键
```

- [ ] **Step 3: typecheck + commit**

```bash
pnpm --filter auvezy-terminal-remote-frontend typecheck
git add frontend/src/i18n/messages.ts
git commit -m "feat(file-browser): 阶段 5.3 — i18n files.* 键(zh-CN + en-US)"
```

---

### Task 5.4 — `FileBrowserSheet` 基础结构(无高亮 + 无搜索)

**Files:**
- Create: `frontend/src/components/files/FileBrowserSheet.tsx`
- Create: `frontend/src/components/files/FileBrowserSheet.module.scss`
- Create: `frontend/src/components/files/Breadcrumb.tsx`
- Create: `frontend/src/components/files/FileList.tsx`
- Create: `frontend/src/components/files/PreviewPane.tsx`
- Create: `frontend/src/components/files/TextPreview.tsx`
- Create: `frontend/src/components/files/ImagePreview.tsx`

- [ ] **Step 1: `FileBrowserSheet.tsx`**(主容器)

```tsx
/**
 * FileBrowserSheet
 *
 * 文件浏览面板。复用 Sheet primitive,桌面两栏,移动单栏栈。
 * 阶段 5:基础列表 + 预览,无搜索无高亮。
 */

import { useEffect, useState, type JSX } from 'react';
import type { FileEntry } from 'auvezy-terminal-remote-shared';
import { Sheet } from '../ui/Sheet.js';
import { useT } from '../../i18n/i18n-context.js';
import { useFiles } from '../../hooks/useFiles.js';
import { Breadcrumb } from './Breadcrumb.js';
import { FileList } from './FileList.js';
import { PreviewPane, type PreviewTarget } from './PreviewPane.js';
import s from './FileBrowserSheet.module.scss';

export interface FileBrowserSheetProps {
  open: boolean;
  onOpenChange: (next: boolean) => void;
  instanceId: string;
}

export function FileBrowserSheet({ open, onOpenChange, instanceId }: FileBrowserSheetProps): JSX.Element {
  const t = useT();
  const files = useFiles(instanceId);
  const [path, setPath] = useState<string | undefined>(undefined);
  const [cwd, setCwd] = useState<string>('');
  const [parent, setParent] = useState<string | null>(null);
  const [entries, setEntries] = useState<FileEntry[]>([]);
  const [showHidden, setShowHidden] = useState(false);
  const [preview, setPreview] = useState<PreviewTarget | null>(null);
  const [error, setError] = useState<string | null>(null);

  useEffect(() => {
    if (!open) return;
    let cancelled = false;
    setError(null);
    files.list(path).then((r) => {
      if (cancelled) return;
      setCwd(r.cwd);
      setEntries(r.entries);
      setParent(r.parent);
      setPath(r.path);
    }).catch((e: Error & { code?: string }) => {
      if (!cancelled) setError(e.code ?? 'UNKNOWN');
    });
    return () => { cancelled = true; };
  }, [open, path, instanceId, files]);

  const onEntryClick = (e: FileEntry): void => {
    const full = e.kind === 'dir' || e.kind === 'symlink'
      ? `${path}/${e.name}`
      : `${path}/${e.name}`;
    if (e.kind === 'dir') {
      setPath(full);
      return;
    }
    if (e.previewable === 'text') {
      setPreview({ kind: 'text', path: full, name: e.name });
    } else if (e.previewable === 'image') {
      setPreview({ kind: 'image', path: full, name: e.name, size: e.size });
    } else {
      setPreview({ kind: 'none', path: full, name: e.name, size: e.size });
    }
  };

  const visibleEntries = showHidden ? entries : entries.filter((e) => !e.hidden);

  return (
    <Sheet open={open} onOpenChange={onOpenChange} title={t('files.title')}>
      <div className={s.root}>
        <Breadcrumb
          cwd={cwd} path={path ?? cwd} parent={parent}
          onJump={(p) => setPath(p)}
          showHidden={showHidden}
          onToggleHidden={() => setShowHidden((v) => !v)}
        />
        <div className={s.body}>
          <FileList
            entries={visibleEntries}
            error={error}
            onEntryClick={onEntryClick}
          />
          <PreviewPane
            instanceId={instanceId}
            target={preview}
            onClose={() => setPreview(null)}
          />
        </div>
      </div>
    </Sheet>
  );
}
```

- [ ] **Step 2: `Breadcrumb.tsx`**

```tsx
import type { JSX } from 'react';
import { useT } from '../../i18n/i18n-context.js';
import s from './FileBrowserSheet.module.scss';

export interface BreadcrumbProps {
  cwd: string;
  path: string;
  parent: string | null;
  onJump: (path: string) => void;
  showHidden: boolean;
  onToggleHidden: () => void;
}

export function Breadcrumb(props: BreadcrumbProps): JSX.Element {
  const t = useT();
  const segments = props.path.split('/').filter((x) => x.length > 0);
  return (
    <div className={s.breadcrumb}>
      <button onClick={() => props.onJump(props.cwd)}>{t('files.toolbar.cwd')}</button>
      <button onClick={() => props.parent && props.onJump(props.parent)} disabled={!props.parent}>
        ↑ {t('files.toolbar.up')}
      </button>
      <span className={s.segs}>
        {segments.map((seg, i) => (
          <span key={i}>/{seg}</span>
        ))}
      </span>
      <label className={s.toggle}>
        <input type="checkbox" checked={props.showHidden} onChange={props.onToggleHidden} />
        {t('files.toolbar.showHidden')}
      </label>
    </div>
  );
}
```

- [ ] **Step 3: `FileList.tsx`**

```tsx
import type { JSX } from 'react';
import type { FileEntry } from 'auvezy-terminal-remote-shared';
import { useT } from '../../i18n/i18n-context.js';
import s from './FileBrowserSheet.module.scss';

export interface FileListProps {
  entries: FileEntry[];
  error: string | null;
  onEntryClick: (e: FileEntry) => void;
}

export function FileList({ entries, error, onEntryClick }: FileListProps): JSX.Element {
  const t = useT();
  if (error) return <div className={s.error}>{error}</div>;
  if (entries.length === 0) return <div className={s.empty}>{t('files.empty')}</div>;
  return (
    <ul className={s.list}>
      {entries.map((e) => (
        <li key={e.name} onClick={() => onEntryClick(e)}>
          <span className={s.icon}>{iconFor(e)}</span>
          <span className={s.name}>{e.name}</span>
          <span className={s.size}>{e.kind === 'dir' ? '' : formatBytes(e.size)}</span>
        </li>
      ))}
    </ul>
  );
}

function iconFor(e: FileEntry): string {
  if (e.kind === 'dir') return '📁';
  if (e.kind === 'symlink') return '🔗';
  if (e.previewable === 'image') return '🖼';
  if (e.previewable === 'text') return '📄';
  return '⬜';
}

function formatBytes(n: number): string {
  if (n < 1024) return `${n} B`;
  if (n < 1024 * 1024) return `${(n / 1024).toFixed(1)} KB`;
  return `${(n / 1024 / 1024).toFixed(1)} MB`;
}
```

- [ ] **Step 4: `PreviewPane.tsx` / `TextPreview.tsx` / `ImagePreview.tsx`**

```tsx
// PreviewPane.tsx
import type { JSX } from 'react';
import { TextPreview } from './TextPreview.js';
import { ImagePreview } from './ImagePreview.js';
import { useT } from '../../i18n/i18n-context.js';
import s from './FileBrowserSheet.module.scss';

export type PreviewTarget =
  | { kind: 'text'; path: string; name: string }
  | { kind: 'image'; path: string; name: string; size: number }
  | { kind: 'none'; path: string; name: string; size: number };

export interface PreviewPaneProps {
  instanceId: string;
  target: PreviewTarget | null;
  onClose: () => void;
}

export function PreviewPane({ instanceId, target, onClose }: PreviewPaneProps): JSX.Element {
  const t = useT();
  if (!target) return <div className={s.preview} />;
  return (
    <div className={s.preview}>
      <header>
        <strong>{target.name}</strong>
        <button onClick={onClose}>×</button>
      </header>
      {target.kind === 'text' && <TextPreview instanceId={instanceId} path={target.path} />}
      {target.kind === 'image' && <ImagePreview instanceId={instanceId} path={target.path} />}
      {target.kind === 'none' && <div className={s.unsupported}>{t('files.preview.binary')}</div>}
    </div>
  );
}
```

```tsx
// TextPreview.tsx (阶段 5:无高亮纯 <pre>;阶段 6 接 Shiki)
import { useEffect, useState, type JSX } from 'react';
import { useFiles } from '../../hooks/useFiles.js';
import { useT } from '../../i18n/i18n-context.js';
import s from './FileBrowserSheet.module.scss';

export function TextPreview({ instanceId, path }: { instanceId: string; path: string }): JSX.Element {
  const t = useT();
  const files = useFiles(instanceId);
  const [content, setContent] = useState<string>('');
  const [truncated, setTruncated] = useState(false);
  const [err, setErr] = useState<string | null>(null);

  useEffect(() => {
    let cancelled = false;
    files.read(path).then((r) => {
      if (!cancelled) { setContent(r.content); setTruncated(r.truncated); }
    }).catch((e: Error & { code?: string }) => {
      if (!cancelled) setErr(e.code ?? 'UNKNOWN');
    });
    return () => { cancelled = true; };
  }, [path, files]);

  if (err) return <div className={s.error}>{err}</div>;
  return (
    <>
      {truncated && <div className={s.notice}>{t('files.preview.truncated')}</div>}
      <pre className={s.textPre}>{content}</pre>
    </>
  );
}
```

```tsx
// ImagePreview.tsx
import { useState, type JSX } from 'react';
import { rawUrl } from '../../services/files-api.js';
import { useT } from '../../i18n/i18n-context.js';
import s from './FileBrowserSheet.module.scss';

export function ImagePreview({ instanceId, path }: { instanceId: string; path: string }): JSX.Element {
  const t = useT();
  const [failed, setFailed] = useState(false);
  if (failed) return <div className={s.error}>{t('files.preview.binary')}</div>;
  return (
    <img
      className={s.image}
      src={rawUrl(instanceId, path)}
      onError={() => setFailed(true)}
      alt={path}
    />
  );
}
```

- [ ] **Step 5: `FileBrowserSheet.module.scss` 基础布局**

```scss
.root { display: flex; flex-direction: column; height: 100%; }
.breadcrumb {
  display: flex; gap: 8px; padding: 8px 12px; align-items: center; flex-wrap: wrap;
  border-bottom: 1px solid var(--color-border, #ddd);
  button { padding: 4px 10px; min-height: 32px; }
  .segs { font-family: monospace; color: var(--color-text-muted, #666); overflow-x: auto; }
  .toggle { margin-left: auto; display: flex; gap: 4px; align-items: center; }
}
.body {
  flex: 1; min-height: 0;
  display: grid; grid-template-columns: 300px 1fr;
  @media (max-width: 768px) { grid-template-columns: 1fr; }
}
.list {
  margin: 0; padding: 0; list-style: none; overflow-y: auto;
  border-right: 1px solid var(--color-border, #ddd);
  li {
    display: grid; grid-template-columns: 24px 1fr auto;
    gap: 8px; padding: 8px 12px; cursor: pointer; min-height: 44px;
    align-items: center;
    &:hover { background: var(--color-hover, #f4f4f4); }
  }
  .size { color: var(--color-text-muted, #888); font-size: 12px; }
}
.preview {
  display: flex; flex-direction: column; min-height: 0; overflow: hidden;
  header {
    display: flex; justify-content: space-between; align-items: center;
    padding: 8px 12px; border-bottom: 1px solid var(--color-border, #ddd);
    button { background: none; border: none; font-size: 18px; cursor: pointer; }
  }
  .textPre {
    flex: 1; margin: 0; padding: 12px; overflow: auto;
    font-family: ui-monospace, monospace; font-size: 13px; line-height: 1.5;
  }
  .image { max-width: 100%; max-height: 100%; object-fit: contain; }
}
.notice { padding: 6px 12px; background: var(--color-warn-bg, #fff8c4); font-size: 12px; }
.error { padding: 12px; color: var(--color-error, #c33); }
.empty { padding: 24px; text-align: center; color: var(--color-text-muted, #888); }
.unsupported { padding: 24px; text-align: center; color: var(--color-text-muted, #888); }
```

- [ ] **Step 6: typecheck + commit**

```bash
pnpm --filter auvezy-terminal-remote-frontend typecheck
git add frontend/src/components/files/
git commit -m "feat(file-browser): 阶段 5.4 — FileBrowserSheet 基础结构(无高亮无搜索)

包括 Breadcrumb / FileList / PreviewPane / TextPreview / ImagePreview。
桌面 300px+1fr 双栏,移动单栏栈(media query)。"
```

---

### Task 5.5 — 接入 ConsolePage(IconFolder 按钮)

**Files:**
- Modify: `frontend/src/pages/ConsolePage.tsx`

- [ ] **Step 1: 引入 + state + 按钮 + sheet**

```ts
// 顶部 imports 区追加
import { IconFolder } from '@tabler/icons-react';
import { FileBrowserSheet } from '../components/files/FileBrowserSheet.js';

// ConsolePage 组件函数体内、其它 useState 旁追加
const [fileBrowserOpen, setFileBrowserOpen] = useState(false);
```

在已有 IconButton(Share / Settings / Search)旁加:

```tsx
<IconButton onClick={() => setFileBrowserOpen(true)} aria-label={t('files.title')}>
  <IconFolder />
</IconButton>
```

在组件 return 末尾(其它 sheet 之后)加:

```tsx
{currentInstanceId && (
  <FileBrowserSheet
    open={fileBrowserOpen}
    onOpenChange={setFileBrowserOpen}
    instanceId={currentInstanceId}
  />
)}
```

> `currentInstanceId` 需要从 useInstances / app store 取——读 ConsolePage 现有代码看怎么拿"当前激活实例 id"。如果当前实现是 `useAppStore((s) => s.activeInstanceId)` 则同样写法。

- [ ] **Step 2: typecheck**

```bash
pnpm --filter auvezy-terminal-remote-frontend typecheck
```

- [ ] **Step 3: commit**

```bash
git add frontend/src/pages/ConsolePage.tsx
git commit -m "feat(file-browser): 阶段 5.5 — IconFolder 按钮挂入 ConsolePage 顶栏"
```

---

### Task 5.6 — smoke test(阶段 5)

> 按 CLAUDE.md 重启流程做 smoke,**完后 kill broker + 确认端口释放**。

- [ ] **Step 1: 启 broker(若未启)**

```bash
pgrep -af "node.*cli\.js broker|tsx.*cli\.ts broker"
```

如无,启:

```bash
nohup pnpm --filter auvezy-terminal-remote exec tsx src/cli.ts broker start > /tmp/atr-broker.log 2>&1 &
sleep 6
curl --noproxy '*' http://127.0.0.1:3737/api/health
```

- [ ] **Step 2: 启 vite**

```bash
nohup pnpm --filter auvezy-terminal-remote-frontend dev > /tmp/atr-vite.log 2>&1 &
sleep 4
```

- [ ] **Step 3: 浏览器手工验证**

打开 `http://localhost:5173`,登录 → 创建实例(cwd = 本仓库根)→ 点 IconFolder → 应能列出 `README.md` / `backend` / `frontend` / `shared` 等条目 → 点 `README.md` 显示纯文本预览 → 点 `frontend/public/icons/<某 png>` 显示图片。

- [ ] **Step 4: 清理**

```bash
pgrep -af "node.*cli\.js broker|tsx.*cli\.ts broker"
# 把上一步看到的 broker PID 替进去
kill <broker-pid>
pgrep -f "vite" | xargs -r kill
sleep 2
ss -tln | grep -E ':(3737|5173)' || echo "ports released"
```

- [ ] **Step 5: 进度文档 + commit**

`docs/plans/file-browser/progress/05-阶段5-前端基础.md`:

```markdown
# 阶段 5 — 前端基础 FileBrowserSheet

> 状态:✅ 已完成(<日期>)

## 完成项

- 5.1 — files-api.ts(list/read/stat/rawUrl/streamSearch)
- 5.2 — useFiles hook(路径缓存 + 切实例重置)
- 5.3 — i18n files.* zh-CN/en-US
- 5.4 — FileBrowserSheet + 5 子组件 + scss
- 5.5 — ConsolePage IconFolder 按钮挂载
- 5.6 — smoke 通过(列目录 + 文本预览 + 图片预览)

## 已知未做

- 语法高亮(阶段 6)
- 文件搜索 UI(阶段 7)
```

```bash
git add docs/plans/file-browser/progress/05-阶段5-前端基础.md
git commit -m "docs(file-browser): 阶段 5 进度文档"
```

---

## 阶段 6 — Shiki 集成

阶段产物:文本预览自带语法高亮 + 主题跟随 + 超大/未知降级。

### Task 6.1 — 装依赖

- [ ] **Step 1**

```bash
pnpm --filter auvezy-terminal-remote-frontend add shiki
```

- [ ] **Step 2: 查实际装到的版本 + commit**

```bash
grep '"shiki"' frontend/package.json
git add frontend/package.json pnpm-lock.yaml
git commit -m "chore(file-browser): 阶段 6.1 — 装 shiki(prod dep)"
```

---

### Task 6.2 — `syntax-highlight.ts` 写测试

**Files:**
- Create: `frontend/src/utils/syntax-highlight.test.ts`

- [ ] **Step 1: 写测试**

```ts
import { describe, it, expect, vi } from 'vitest';
import { highlight, escapeHtmlForTest } from './syntax-highlight.js';

describe('highlight', () => {
  it('超过 200 KB 文本走 escapeHtml 降级(不调 shiki)', async () => {
    const big = 'x'.repeat(201 * 1024);
    const html = await highlight(big, 'ts', 'github-dark');
    expect(html).toContain('x'); // 至少包含原文一部分
    expect(html).not.toContain('shiki'); // shiki 输出会有 <pre class="shiki"
  });

  it('未知 lang 走降级 escapeHtml', async () => {
    const html = await highlight('hello <world>', 'xyz-not-real', 'github-dark');
    expect(html).toContain('&lt;world&gt;');
  });

  it('escapeHtml 正确转义五大字符', () => {
    expect(escapeHtmlForTest('<a&b>"c\'')).toBe('&lt;a&amp;b&gt;&quot;c&#39;');
  });
});
```

- [ ] **Step 2: 跑失败 + commit**

```bash
pnpm --filter auvezy-terminal-remote-frontend exec vitest run src/utils/syntax-highlight.test.ts
git add frontend/src/utils/syntax-highlight.test.ts
git commit -m "test(file-browser): 阶段 6.2 — syntax-highlight 失败测试"
```

---

### Task 6.3 — `syntax-highlight.ts` 实现

**Files:**
- Create: `frontend/src/utils/syntax-highlight.ts`
- Create: `frontend/src/utils/lang-map.ts`

> ⚠️ 实施注意:**Shiki API 形态以 `pnpm add shiki` 装到的版本为准**(README / changelog 优先于本文档伪代码)。如果当前主线提供 `getSingletonHighlighter`,优先用;否则用 `getHighlighter`。本计划落锚于 1.x 主线 API(`getSingletonHighlighter` + `codeToHtml`);若 README 已变,调整对应调用,保持本模块对外契约不变。

- [ ] **Step 1: 看一眼 shiki README**

```bash
cat /mnt/d/github/open-terminal-remote/node_modules/shiki/README.md | head -100
```

确认是否有 `getSingletonHighlighter` / `codeToHtml` 等导出。如果是 1.x 主线,继续 step 2;如果 API 名变了,在 step 2 中改对应调用并补 ADR-006。

- [ ] **Step 2: `lang-map.ts`**

```ts
/**
 * 后端 lang → Shiki bundleId 映射。未知 → 'txt'(等价无高亮)。
 */
export function toShikiLang(backendLang: string): string {
  return KNOWN.has(backendLang) ? backendLang : 'txt';
}

const KNOWN = new Set([
  'ts','tsx','js','jsx','markdown','yaml','json','toml',
  'python','go','rust','ruby','php','java','kotlin','swift',
  'c','cpp','csharp','shell','html','css','scss','sass','less',
  'xml','sql',
]);
```

- [ ] **Step 3: `syntax-highlight.ts`**

```ts
/**
 * syntax-highlight:Shiki lazy load + 降级
 *
 * - >200 KB 文本 → escapeHtml 直出
 * - 未知 lang → escapeHtml 直出
 * - 任何加载失败 → escapeHtml 不抛
 */

import { toShikiLang } from './lang-map.js';

const MAX_HIGHLIGHT_BYTES = 200 * 1024;

let highlighterPromise: Promise<unknown> | null = null;
const loaded = new Set<string>();

export async function highlight(
  code: string,
  backendLang: string,
  theme: 'github-dark' | 'github-light',
): Promise<string> {
  if (code.length > MAX_HIGHLIGHT_BYTES) return wrapPre(escapeHtml(code));
  const lang = toShikiLang(backendLang);
  if (lang === 'txt') return wrapPre(escapeHtml(code));

  try {
    const shiki = await import('shiki');
    // Shiki 1.x:getSingletonHighlighter;若改名按当时 API 调
    type ShikiNS = {
      getSingletonHighlighter?: (opts: { themes: string[]; langs: string[] }) => Promise<{
        loadLanguage(l: string): Promise<void>;
        codeToHtml(code: string, opts: { lang: string; theme: string }): string;
      }>;
      getHighlighter?: (opts: { themes: string[]; langs: string[] }) => Promise<{
        loadLanguage(l: string): Promise<void>;
        codeToHtml(code: string, opts: { lang: string; theme: string }): string;
      }>;
    };
    const ns = shiki as unknown as ShikiNS;
    const getH = ns.getSingletonHighlighter ?? ns.getHighlighter;
    if (!getH) return wrapPre(escapeHtml(code));
    if (!highlighterPromise) {
      highlighterPromise = getH({ themes: ['github-dark', 'github-light'], langs: [] });
    }
    const h = (await highlighterPromise) as {
      loadLanguage(l: string): Promise<void>;
      codeToHtml(c: string, o: { lang: string; theme: string }): string;
    };
    if (!loaded.has(lang)) {
      await h.loadLanguage(lang);
      loaded.add(lang);
    }
    return h.codeToHtml(code, { lang, theme });
  } catch {
    return wrapPre(escapeHtml(code));
  }
}

function escapeHtml(s: string): string {
  return s
    .replace(/&/g, '&amp;')
    .replace(/</g, '&lt;')
    .replace(/>/g, '&gt;')
    .replace(/"/g, '&quot;')
    .replace(/'/g, '&#39;');
}

function wrapPre(html: string): string {
  return `<pre><code>${html}</code></pre>`;
}

/** 测试用导出 */
export const escapeHtmlForTest = escapeHtml;
```

- [ ] **Step 4: 跑测试**

```bash
pnpm --filter auvezy-terminal-remote-frontend exec vitest run src/utils/syntax-highlight.test.ts
```

Expected: PASS(3/3)。

- [ ] **Step 5: commit**

```bash
git add frontend/src/utils/syntax-highlight.ts frontend/src/utils/lang-map.ts
git commit -m "feat(file-browser): 阶段 6.3 — Shiki 包装 + 降级路径

>200 KB / 未知 lang / 加载失败全部降级 escapeHtml 不抛。Shiki API
按装到的版本动态探测(getSingletonHighlighter / getHighlighter 二选一)。"
```

---

### Task 6.4 — `TextPreview` 接入高亮

**Files:**
- Modify: `frontend/src/components/files/TextPreview.tsx`

- [ ] **Step 1: 改 TextPreview**

```tsx
import { useEffect, useState, type JSX } from 'react';
import { useFiles } from '../../hooks/useFiles.js';
import { useT } from '../../i18n/i18n-context.js';
import { highlight } from '../../utils/syntax-highlight.js';
import s from './FileBrowserSheet.module.scss';

const MAX_HIGHLIGHT_BYTES = 200 * 1024;

export function TextPreview({ instanceId, path }: { instanceId: string; path: string }): JSX.Element {
  const t = useT();
  const files = useFiles(instanceId);
  const [html, setHtml] = useState<string>('');
  const [truncated, setTruncated] = useState(false);
  const [highlightOff, setHighlightOff] = useState(false);
  const [err, setErr] = useState<string | null>(null);
  const theme = matchMedia('(prefers-color-scheme: dark)').matches ? 'github-dark' : 'github-light';

  useEffect(() => {
    let cancelled = false;
    setErr(null); setHtml(''); setTruncated(false); setHighlightOff(false);
    files.read(path).then(async (r) => {
      if (cancelled) return;
      setTruncated(r.truncated);
      setHighlightOff(r.content.length > MAX_HIGHLIGHT_BYTES);
      const rendered = await highlight(r.content, r.lang, theme);
      if (!cancelled) setHtml(rendered);
    }).catch((e: Error & { code?: string }) => {
      if (!cancelled) setErr(e.code ?? 'UNKNOWN');
    });
    return () => { cancelled = true; };
  }, [path, files, theme]);

  if (err) return <div className={s.error}>{err}</div>;
  return (
    <>
      {truncated && <div className={s.notice}>{t('files.preview.truncated')}</div>}
      {highlightOff && <div className={s.notice}>{t('files.preview.highlightOff')}</div>}
      <div className={s.textPre} dangerouslySetInnerHTML={{ __html: html }} />
    </>
  );
}
```

> 注:`dangerouslySetInnerHTML` 是必要的(Shiki 输出 HTML)。`highlight()` 内部已确保所有非 Shiki 路径都走 `escapeHtml`,而 Shiki 自己产出的 HTML 是可信的——无 XSS 风险。

- [ ] **Step 2: 验 vite build 主 bundle 增量 + commit**

```bash
pnpm --filter auvezy-terminal-remote-frontend build 2>&1 | tee /tmp/atr-build-stats.txt
```

观察主 chunk(`index-xxx.js` gz)大小。**若主 bundle gz 增量 > 50 KB**,触发 ADR-004 回退条款(改 Prism),停止本阶段并写 ADR-006 + 重新设计;否则:

```bash
git add frontend/src/components/files/TextPreview.tsx
git commit -m "feat(file-browser): 阶段 6.4 — TextPreview 接入 Shiki 高亮 + 主题跟随"
```

---

### Task 6.5 — 写 ADR-006 + 进度文档

**Files:**
- Create: `docs/plans/file-browser/adrs/006-shiki-version-pin.md`
- Create: `docs/plans/file-browser/progress/06-阶段6-语法高亮.md`

- [ ] **Step 1: ADR-006**

```markdown
# ADR-006: Shiki 实际选用版本与 API 调用形态

## 状态

已采纳(<实施日期>)

## 上下文

ADR-004 决定选 Shiki,但 Shiki 0.x → 1.x → 2.x API 变迁多次,设计阶段无法锁死调用形态。本 ADR 在阶段 6 实施完成后落档实际版本与最终调用方式,避免后人误信 design 中的伪代码。

## 决策

- **版本**:`<填实际版本号,从 frontend/package.json 拿>`
- **API**:`<填实际用到的 export,如 getSingletonHighlighter 或 getHighlighter>` + `codeToHtml(code, { lang, theme })`
- **主 bundle 增量**:`<填 vite build 量到的实际 gz 增量>` KB(<50 KB 阈值,通过)
- **首次 lang chunk**:`<填某常用 lang 如 ts 的 chunk gz 大小>` KB

## 理由

API 实测时为准。<50 KB 主 bundle 增量满足 ADR-004 验收。

## 后果

- ✅ 后续维护者直接看本 ADR 知道当前用什么 API,不被 design 伪代码误导。
- ⚠️ shiki 升级 major 版本时回头检查本 ADR + 更新 syntax-highlight.ts。
```

- [ ] **Step 2: 进度文档**

```markdown
# 阶段 6 — Shiki 语法高亮

> 状态:✅ 已完成(<日期>)

## 完成项

- 6.1 — pnpm add shiki(版本 <填>)
- 6.2 / 6.3 — syntax-highlight.ts + lang-map.ts + 3 测试
- 6.4 — TextPreview 接入高亮,主题跟随 prefers-color-scheme
- 6.5 — ADR-006 落 + 主 bundle 增量验证通过

## 验证记录

- vite build 主 bundle gz 增量:<填>(< 50 KB 阈值)
- 首屏:无延迟(shiki 走 dynamic import)
```

- [ ] **Step 3: commit**

```bash
git add docs/plans/file-browser/adrs/006-shiki-version-pin.md docs/plans/file-browser/progress/06-阶段6-语法高亮.md
git commit -m "docs(file-browser): 阶段 6 — ADR-006 锁版本与 API 形态 + 进度"
```

---

## 阶段 7 — 搜索 UI

阶段产物:FileBrowserSheet 顶部搜索框 → 流式结果 → 点命中跳预览。

### Task 7.1 — `SearchBox.tsx` + `SearchResults.tsx`

**Files:**
- Create: `frontend/src/components/files/SearchBox.tsx`
- Create: `frontend/src/components/files/SearchResults.tsx`

- [ ] **Step 1: `SearchBox.tsx`**

```tsx
import { useEffect, useRef, useState, type JSX } from 'react';
import { useT } from '../../i18n/i18n-context.js';
import s from './FileBrowserSheet.module.scss';

export interface SearchBoxProps {
  value: string;
  caseSensitive: boolean;
  regex: boolean;
  onChange: (v: string) => void;
  onToggleCase: () => void;
  onToggleRegex: () => void;
  onCancel: () => void;
  scanning: boolean;
  scanned: number;
  hits: number;
}

export function SearchBox(props: SearchBoxProps): JSX.Element {
  const t = useT();
  return (
    <div className={s.searchBox}>
      <input
        type="text"
        placeholder={t('files.search.placeholder')}
        value={props.value}
        onChange={(e) => props.onChange(e.target.value)}
      />
      <label><input type="checkbox" checked={props.caseSensitive} onChange={props.onToggleCase} /> Aa</label>
      <label><input type="checkbox" checked={props.regex} onChange={props.onToggleRegex} /> .*</label>
      {props.scanning && (
        <span className={s.scanning}>
          {t('files.search.scanning')
            .replace('{scanned}', String(props.scanned))
            .replace('{hits}', String(props.hits))}
        </span>
      )}
      {props.scanning && <button onClick={props.onCancel}>×</button>}
    </div>
  );
}
```

- [ ] **Step 2: `SearchResults.tsx`**

```tsx
import type { JSX } from 'react';
import type { SearchEvent } from 'auvezy-terminal-remote-shared';
import { useT } from '../../i18n/i18n-context.js';
import s from './FileBrowserSheet.module.scss';

export interface SearchResultsProps {
  hits: SearchEvent[];
  truncated: boolean;
  onPick: (hit: SearchEvent) => void;
}

export function SearchResults({ hits, truncated, onPick }: SearchResultsProps): JSX.Element {
  const t = useT();
  if (hits.length === 0) return <div className={s.empty}>{t('files.search.empty')}</div>;
  const nameHits = hits.filter((h) => h.kind === 'name');
  const contentHits = hits.filter((h) => h.kind === 'content');
  return (
    <ul className={s.searchList}>
      {nameHits.map((h, i) => (
        <li key={`n-${i}`} onClick={() => onPick(h)}>
          📄 <span className={s.name}>{h.path}</span>
        </li>
      ))}
      {contentHits.map((h, i) => {
        if (h.kind !== 'content') return null;
        const before = h.preview.slice(0, h.matchStart);
        const match = h.preview.slice(h.matchStart, h.matchEnd);
        const after = h.preview.slice(h.matchEnd);
        return (
          <li key={`c-${i}`} onClick={() => onPick(h)}>
            <span className={s.path}>{h.path}:{h.line}</span>
            <code>{before}<mark>{match}</mark>{after}</code>
          </li>
        );
      })}
      {truncated && <li className={s.notice}>{t('files.search.truncated')}</li>}
    </ul>
  );
}
```

- [ ] **Step 3: scss 追加(同一 .module.scss)**

```scss
.searchBox {
  display: flex; gap: 8px; padding: 8px 12px; align-items: center; flex-wrap: wrap;
  border-bottom: 1px solid var(--color-border, #ddd);
  input[type="text"] { flex: 1; min-height: 36px; padding: 4px 8px; }
  label { display: flex; gap: 2px; align-items: center; font-size: 12px; }
  .scanning { font-size: 12px; color: var(--color-text-muted, #888); }
}
.searchList {
  margin: 0; padding: 0; list-style: none; overflow-y: auto;
  li {
    padding: 8px 12px; cursor: pointer;
    border-bottom: 1px solid var(--color-border, #eee);
    &:hover { background: var(--color-hover, #f4f4f4); }
    .path { font-family: monospace; font-size: 11px; color: var(--color-text-muted, #888); display: block; }
    code { display: block; font-size: 12px; }
    mark { background: var(--color-mark, #ffeb3b); }
  }
}
```

- [ ] **Step 4: commit**

```bash
git add frontend/src/components/files/SearchBox.tsx \
        frontend/src/components/files/SearchResults.tsx \
        frontend/src/components/files/FileBrowserSheet.module.scss
git commit -m "feat(file-browser): 阶段 7.1 — SearchBox + SearchResults 组件"
```

---

### Task 7.2 — `FileBrowserSheet` 接入搜索

**Files:**
- Modify: `frontend/src/components/files/FileBrowserSheet.tsx`

- [ ] **Step 1: 在 sheet 内 state 区追加搜索状态**

```tsx
const [searchQ, setSearchQ] = useState('');
const [caseSensitive, setCaseSensitive] = useState(false);
const [regex, setRegex] = useState(false);
const [hits, setHits] = useState<SearchEvent[]>([]);
const [scanning, setScanning] = useState(false);
const [scanned, setScanned] = useState(0);
const [searchTruncated, setSearchTruncated] = useState(false);
const handleRef = useRef<{ cancel(): void } | null>(null);
```

并 `import { streamSearch } from '../../services/files-api.js';` + `import { useRef } from 'react';` + `import type { SearchEvent } from 'auvezy-terminal-remote-shared';`。

- [ ] **Step 2: 加 useEffect 触发搜索**

```tsx
useEffect(() => {
  handleRef.current?.cancel();
  if (searchQ.length < 3) {
    setHits([]); setScanning(false); setScanned(0); setSearchTruncated(false);
    return;
  }
  setHits([]); setScanned(0); setSearchTruncated(false); setScanning(true);
  const h = streamSearch(instanceId,
    { q: searchQ, mode: 'both', caseSensitive, regex },
    (m) => {
      setHits((prev) => [...prev, m]);
      setScanned((n) => n + 1);
    },
    (d) => { setScanning(false); setSearchTruncated(d.truncated); },
    () => { setScanning(false); },
  );
  handleRef.current = h;
  return () => h.cancel();
}, [searchQ, caseSensitive, regex, instanceId]);
```

- [ ] **Step 3: 在 Breadcrumb 下加 SearchBox + 条件渲染 SearchResults / FileList**

```tsx
<SearchBox
  value={searchQ}
  caseSensitive={caseSensitive}
  regex={regex}
  onChange={setSearchQ}
  onToggleCase={() => setCaseSensitive((v) => !v)}
  onToggleRegex={() => setRegex((v) => !v)}
  onCancel={() => { handleRef.current?.cancel(); setScanning(false); }}
  scanning={scanning}
  scanned={scanned}
  hits={hits.length}
/>
<div className={s.body}>
  {searchQ.length >= 3 ? (
    <SearchResults
      hits={hits}
      truncated={searchTruncated}
      onPick={(h) => {
        if (h.kind === 'name') {
          // 把 name 命中作为预览(text 或 image,由前端 detect 不到 mime,简单走 text)
          setPreview({ kind: 'text', path: h.path, name: h.path });
        } else {
          // 内容命中也按 text 预览打开;精准行跳转不在 MVP 范围(见阶段 7 进度文档"已知未做")
          setPreview({ kind: 'text', path: h.path, name: `${h.path}:${h.line}` });
        }
        setSearchQ('');  // 关闭搜索回 list
      }}
    />
  ) : (
    <FileList entries={visibleEntries} error={error} onEntryClick={onEntryClick} />
  )}
  <PreviewPane instanceId={instanceId} target={preview} onClose={() => setPreview(null)} />
</div>
```

- [ ] **Step 4: typecheck + commit**

```bash
pnpm --filter auvezy-terminal-remote-frontend typecheck
git add frontend/src/components/files/FileBrowserSheet.tsx
git commit -m "feat(file-browser): 阶段 7.2 — FileBrowserSheet 接入搜索流

>= 3 char 自动触发 mode=both,EventSource 实时流;切关键字自动 cancel
旧 stream。命中点击 → 关搜索 + 打开预览。"
```

---

### Task 7.3 — 进度文档

**Files:**
- Create: `docs/plans/file-browser/progress/07-阶段7-搜索UI.md`

- [ ] **Step 1: 写**

```markdown
# 阶段 7 — 搜索 UI

> 状态:✅ 已完成(<日期>)

## 完成项

- 7.1 — SearchBox + SearchResults 组件
- 7.2 — FileBrowserSheet 接入 streamSearch,>=3 char 触发,name+content 双模式合并展示

## 已知未做(留给阶段 8 / 后续)

- 命中行跳转(需要 Shiki 输出 data-line + scrollIntoView,本阶段先把 path 喂给 TextPreview 即可)
- 文件搜索的 .gitignore 集成(YAGNI)
```

- [ ] **Step 2: commit**

```bash
git add docs/plans/file-browser/progress/07-阶段7-搜索UI.md
git commit -m "docs(file-browser): 阶段 7 进度文档"
```

---

## 阶段 8 — smoke + 性能调优 + 文档收尾

### Task 8.1 — 全链路 smoke

按 CLAUDE.md broker 重启流程跑完整 smoke:

- [ ] **Step 1: 启 broker + vite**

```bash
nohup pnpm --filter auvezy-terminal-remote exec tsx src/cli.ts broker start > /tmp/atr-broker.log 2>&1 &
sleep 6
curl --noproxy '*' http://127.0.0.1:3737/api/health

nohup pnpm --filter auvezy-terminal-remote-frontend dev > /tmp/atr-vite.log 2>&1 &
sleep 4
```

- [ ] **Step 2: 浏览器手工跑 9 个用例**(参见 design.md §10.3)

逐个验证:
1. 列 repo 根
2. 预览 README.md(应有 Markdown 高亮)
3. 上级目录(应走到 repo 父目录,过 policy 通过即列;否则按钮禁用)
4. 回 cwd 按钮
5. 搜索 `checkWorkdir` → 应看到 name + content 命中
6. 取消搜索(改关键字到 0 char)
7. 试图 `/api/files/read?path=/etc/passwd`(curl)→ 403 PATH_FORBIDDEN
8. 大文件预览(找一个 >2 MiB 的 log,看到 truncated 提示)
9. 图片预览(`frontend/public/icons/*.png`)

- [ ] **Step 3: 清理验证(强制)**

```bash
pgrep -af "node.*cli\.js broker|tsx.*cli\.ts broker"
# 把 broker PID 替进去
kill <broker-pid>
pgrep -f "vite" | xargs -r kill
sleep 3
ss -tln | grep -E ':(3737|5173)' && echo "FAIL: 端口未释放" || echo "OK: ports released"
```

如端口未释放,**禁止进入 8.2**,先排查残留进程。

- [ ] **Step 4: smoke log 落 progress**

`docs/plans/file-browser/progress/08-阶段8-smoke收尾.md`:

```markdown
# 阶段 8 — smoke + 收尾

> 状态:✅ 已完成(<日期>)

## smoke 结果

| # | 用例 | 结果 |
|---|---|---|
| 1 | 列 repo 根 | ✅ |
| 2 | 预览 README.md + 高亮 | ✅ |
| 3 | 上级目录 | ✅ |
| 4 | 回 cwd | ✅ |
| 5 | 搜索 checkWorkdir(name+content)| ✅ |
| 6 | 取消搜索 | ✅ |
| 7 | /etc/passwd 拒(403)| ✅ |
| 8 | 大文件 truncated | ✅ |
| 9 | 图片预览 | ✅ |

## 端口释放

`ss -tln` 确认 3737 / 5173 已释放。

## 待后续

- 命中行精准跳转(需 Shiki data-line)
- .gitignore 集成
- 写白名单(若未来加写能力)
```

```bash
git add docs/plans/file-browser/progress/08-阶段8-smoke收尾.md
git commit -m "docs(file-browser): 阶段 8 smoke 结果"
```

---

### Task 8.2 — 更新 progress overview + CHANGELOG

**Files:**
- Modify: `docs/plans/file-browser/progress/00-overview.md`(全部阶段标 ✅)
- Modify: `CHANGELOG.md`(根)

- [ ] **Step 1: 更新 overview 阶段表 status 全标 ✅**

读 `docs/plans/file-browser/progress/00-overview.md`,把 8 个阶段的 `⏳ 未开始` 全改 `✅ 已完成`。

- [ ] **Step 2: CHANGELOG 加 entry**

在 CHANGELOG.md 顶部追加(版本号视实施时已发布的 0.7.x 而定;若尚在 0.7.6 后,记 0.8.0):

```markdown
## 0.8.0(<日期>)

### Features

- **file-browser**:活跃实例新增只读目录浏览 + 文本/图片预览面板。
  - 入口:Console 顶栏 IconFolder 按钮(桌面 + 移动)。
  - 文本预览自带语法高亮(Shiki),主题跟随 prefers-color-scheme。
  - 文件搜索:文件名 + 内容双模式,>=3 char 自动触发,SSE 流式结果。
  - 安全:所有路径过 checkWorkdir,默认 deny 含 `/etc /root /sys /proc`。

详见 `docs/plans/file-browser/`。
```

- [ ] **Step 3: commit**

```bash
git add docs/plans/file-browser/progress/00-overview.md CHANGELOG.md
git commit -m "docs(file-browser): 阶段 8 收尾 — overview 全 ✅ + CHANGELOG"
```

---

## 收尾 checklist

- [ ] 所有阶段 commit 都不含 AI 署名(`git log --oneline` 自检)
- [ ] `pnpm test`(monorepo)全绿
- [ ] `pnpm typecheck` 全绿
- [ ] smoke 后端口已释放(`ss -tln`)
- [ ] ADR-006 已落档(Shiki 版本与 API 形态)
- [ ] CHANGELOG 已更新

完成后可对接发布流程(`publish-atr` skill)。
