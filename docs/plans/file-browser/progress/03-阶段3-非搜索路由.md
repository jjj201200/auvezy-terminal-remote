# 阶段 3 — read-file + 非搜索路由

> 状态:✅ 已完成(2026-05-20)

## 完成项

- 3.1 / 3.2 — `readTextFile` 三段闸(NUL/截断/replacement char)+ 4 测试
- 3.3 — `WorkdirPolicySnapshot` 加内部 `deny` 字段(端点契约不变)
- 3.4 / 3.5 — `/api/files` list/stat/read/raw 4 端点 + 8 个 supertest
- 3.5b — per-IP 限流(120/min)+ 审计日志(action/path/ip/elapsedMs)+ 限流测试
- 3.6 — 挂入 `createBrokerApiRouter`

## 测试增量

backend 测试从 605 → 614 全绿。

## 已知边界

- /raw 错误**不返 JSON**,用 `X-ATR-Error: <code>` header(`<img>` 友好)
- /read 非 regular file → 409 `FILE_TYPE_FORBID`
- /raw 超 8 MiB → 413 `FILE_TOO_LARGE`
- `parent` 越界 policy → null(前端禁用"上级"按钮)
- /api/workdir-policy 端点**不暴露** `deny`(只返 `allow`,沿用安全防线设计)

## 不做(留下一阶段)

- 搜索 search-engine + SSE 端点(阶段 4)
- 前端集成(阶段 5)
