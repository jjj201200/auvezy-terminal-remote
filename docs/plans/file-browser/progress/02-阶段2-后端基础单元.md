# 阶段 2 — 后端基础单元(path-resolver / mime-detect / list-dir)

> 状态:✅ 已完成(2026-05-20)

## 完成项

- 2.1 / 2.2 — `resolveSafePath` 三段闸(resolve → realpath → checkWorkdir)+ 7 测试(commit c0fff19 + 5d45f2b)
- 2.3 — `detectMime` / `detectLang` 纯函数 + 11 测试(commit f73dc9f)
- 2.4 — `listDir`(opendir + lstat)+ 5 测试

## 实施期发现

- macOS / WSL 的 `os.tmpdir()` 可能本身就是 symlink(`/tmp` → `/private/tmp` 等),`realpathSync` 会解到真路径。测试断言用 `realpathSync(cwd)` 而非原始 `cwd` 字符串,保持平台无关。
- `detectMime` 大小写不敏感性靠 `extname().toLowerCase()` 实现,测试用例(`.PNG / .Md`)显式覆盖。

## 不做(留下一阶段)

- read-file(阶段 3 一起)
- 路由挂载(阶段 3)
- 搜索(阶段 4)
