# 阶段 4 — 文件搜索

> 状态:✅ 已完成(2026-05-20)

## 完成项

- 4.1 / 4.2 — `search-engine.ts`:
  - name + content 双模式
  - 8 并发(`SEARCH_CONCURRENCY`)
  - 单文件 100 ms 硬超时(`SEARCH_FILE_TIMEOUT_MS`)
  - 全请求 5 s 硬超时(`SEARCH_TOTAL_TIMEOUT_MS`)
  - IGNORE_DIRS 硬编码(node_modules/.git/dist/...)
  - 字节级 NUL 二进制跳过
  - 跨行 regex 拒(SEARCH_INVALID_Q)
  - 畸形 regex 拒
  - cancelSignal 即时取消
  - 6 个单测
- 4.3 / 4.4 — `/api/files/search` SSE 端点:
  - 参数校验(q 1..200 / mode 枚举 / scope 必须目录)
  - AbortController 绑 req.close
  - match / done / error 三种 SSE event
  - 复用阶段 3 已建的 searchLimiter(per-IP 20/min)
  - 2 个 supertest

## 后端阶段完结

backend 测试从 614 → 622 全绿。所有 `/api/files/*` 端点(list/stat/read/raw/search)可独立工作。下一步前端集成(阶段 5)。
